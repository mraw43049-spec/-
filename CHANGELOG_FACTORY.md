# کارخونه روبی 🏭 — افزودن سیستم جدید

## چیزی که اضافه شد
- دستور متنی `کارخونه روبی` (و مشابه‌هاش) + `/کارخونه` برای باز کردن پنل کارخونه.
- از سطح ۱۰ کاربر باز می‌شود؛ ساخت‌وساز ۱۵۰,۰۰۰ روب‌پوینت + ۳ ساعت انتظار تا افتتاح.
- ۶ خط تولید (آبنبات، ساعت، ساندویچ، ابزار موسیقی، ماهواره، کشتی) که به‌ترتیبِ «مجموع محصول تولیدشده» باز می‌شوند
  (۰ / ۷۵هزار / ۱۲۰هزار / ۱۷۵هزار / ۲۳۰هزار / ۳۰۰هزار).
- هر خط ۳ محصول با هزینه‌ی ساخت و سقف قیمت فروش دقیقاً طبق اعداد درخواستی.
- سه بخش ارتقا: انبار (سطح ۱ تا ۶، ظرفیت ۲۵هزار تا ۷۵هزار)، کارگران (سطح ۱ تا ۱۶ = تعداد سفارش هم‌زمان)،
  دستگاه‌های تولید (سطح ۱ تا ۱۵، زمان یک بچ ۱۰۰٪ از ۲۱ ساعت تا ۷ ساعت). هزینه‌ی اولین ارتقای هرکدام
  ۱۰۰,۰۰۰ روب‌پوینت و هر ارتقای بعدی دوبرابر ارتقای قبلی.
- اکانت‌های پشتیبانی (`ADMIN_IDS`) از سطح ۱۰ به بعد کارخونه‌شون خودکار و بدون زمان ساخته می‌شود و همه‌ی
  خطوط تولید برایشان باز است.

## فرضیاتی که به‌خاطر ابهام پیام درخواست انتخاب شدند (لطفاً بررسی کن)
1. **درصد تولید = درصدی از ظرفیت انبار.** یعنی «تولید ۱۰۰٪» یعنی پر کردن کامل ظرفیت انبار با همون محصول
   (در سطح ۱ = ۲۵,۰۰۰ عدد) در ۲۱ ساعت؛ ۲۵/۵۰/۷۵٪ به همون نسبت تعداد کمتر، هزینه‌ی کمتر و زمان کمتر دارند.
   این تفسیر لازم بود چون پیام اصلی نگفته بود «۱۰۰٪» دقیقاً چند عدد محصوله.
2. **خط «⏳ زمان تولید محصول» در پیام اصلی «۲۰ ثانیه» نوشته شده بود** ولی چون خودِ پیام هم توضیح داده
   «هر محصول اگه ۱۰۰٪ تولید بشه ۲۱ ساعت طول می‌کشه»، این عدد ۲۰ ثانیه رو نمونه/تایپو در نظر گرفتم و به‌جاش
   زمان واقعی بچ ۱۰۰٪ (که با ارتقای دستگاه‌ها کم می‌شه) رو نشون می‌دم.
3. **«تعداد کارگران»** = چند سفارش تولید هم‌زمان می‌تونی داشته باشی (queue slot)، نه سرعت تولید. ارتقاش هم
   مثل بقیه با روب‌پوینت انجام می‌شه (جمله‌ی «روباه‌های زخمی رو به کارگری بگیره» رو فقط به‌عنوان توضیح فضاسازی
   در نظر گرفتم، چون بلافاصله بعدش گفته بودی هزینه‌ی این ارتقا هم ۱۰۰هزار روب‌پوینته).
4. **برداشت محصول تولیدشده = فروش خودکار در همون لحظه** با سقف قیمتی که گفته شده بود («بیشترین قیمت
   فروش»)؛ پیام اصلی توضیح نداده بود محصول تولیدشده باید جدا فروخته بشه یا نه، پس ساده‌ترین حلقه‌ی اقتصادی
   انتخاب شد: دکمه‌ی «💰 برداشت» هم جمع‌آوری می‌کنه هم می‌فروشه و روب‌پوینتش رو به کاربر می‌ده.
5. **«سطح کارخونه»** (بالای پنل، ۱ تا ۱۵) برابر سطح دستگاه‌های تولید در نظر گرفته شد، چون تنها بخشی بود که
   دقیقاً همون بازه‌ی ۱ تا ۱۵ رو داشت.

اگر هرکدوم از این فرضیات با چیزی که تو ذهنت بود فرق داره، بگو تا اصلاحش کنم.

## نکته‌ی مهم دیپلوی
این تغییرات فقط روی فایل‌های پروژه (`bot.py`, `database.py`, `game_logic.py`) اعمال شده. برای فعال‌شدن روی
Railway باید خودت این نسخه رو push کنی روی گیت‌هاب (یا مستقیم آپلود کنی) و Railway ری‌دیپلوی بشه — من به
اینترنت/گیت‌هاب/Railway دسترسی مستقیم ندارم. دیتابیس نیازی به migration دستی نداره؛ `init_db()` همون اول
اجرا ستون‌ها و جدول جدید (`factory_orders`) رو خودش اضافه می‌کنه.
