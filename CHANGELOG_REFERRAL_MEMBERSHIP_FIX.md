# رفع باگ گم شدن رفرال هنگام نیاز به عضویت اجباری

## مشکل
وقتی کاربر جدید با لینک رفرال (`t.me/bot?start=ref_XXXX`) وارد بات می‌شد ولی هنوز عضو کانال‌های
اجباری نبود، تابع `require_membership` قبل از پردازش رفرال، هندلر `/start` را متوقف می‌کرد و
`return` می‌زد. در نتیجه:
- کد رفرال (`context.args[0]`) هرگز به `handle_referral_signup` نمی‌رسید.
- پیام "زیرمجموعه‌ی جدید در انتظار تایید" هیچ‌وقت برای ادمین/پشتیبانی ارسال نمی‌شد.
- بعد از اینکه کاربر عضو کانال می‌شد و روی «عضو شدم، بررسی کن» می‌زد، دیگر پیلود `ref_XXXX`
  در دسترس نبود (چون این یک callback جدید است، نه دستور `/start` با آرگومان)، پس رفرال برای
  همیشه گم می‌شد.

## راه‌حل
- در `start_command`، پیلود رفرال معتبر (`ref_\d+`) بلافاصله در `context.user_data['pending_referral']`
  ذخیره می‌شود؛ این کار **قبل از** چک عضویت انجام می‌شود.
- بعد از عبور موفق از `require_membership`، اگر کاربر جدید باشد، رفرال از `user_data` خوانده و پردازش می‌شود.
- در `membership_callback` (دکمه‌ی «عضو شدم، بررسی کن»)، بعد از تایید عضویت، همان
  `pending_referral` ذخیره‌شده خوانده و پردازش می‌شود تا کاربرانی که مجبور بودند اول عضو کانال شوند
  هم به‌درستی به‌عنوان زیرمجموعه ثبت شوند و پیام تاییدیه برای پشتیبانی ارسال شود.

## نتیجه
حالا صرف نظر از اینکه کاربر جدید بلافاصله عضو کانال‌های اجباری هست یا اول باید عضو بشه، اگر از
لینک اختصاصی یک کاربر وارد بات شود:
1. یک رکورد `Referral` با وضعیت `pending` ساخته می‌شود.
2. پیام «زیرمجموعه‌ی جدید در انتظار تایید» با دکمه‌های تایید/رد برای همه‌ی `ADMIN_IDS` ارسال می‌شود.
