# رفع باگ پنل روبام/روباش + توقف تولید روباه

## ۱) پنل «روبام / روباش» برای بعضی‌ها نمی‌آمد
پنل با یک دکمه‌ی لینک `tg://user?id=...` ارسال می‌شد. تلگرام برای کاربرانی که در
حریم خصوصی‌شان لینک به اکانت را بسته‌اند (یا ربات هنوز نمی‌شناسدشان) این دکمه را با
`BUTTON_USER_PRIVACY_RESTRICTED` رد می‌کند و چون خطا مهار نمی‌شد، کل پنل ارسال نمی‌شد
(هم موقع «روبام» خودِ آن‌ها، هم وقتی کسی روی پیامشان «روباش» می‌زد).
حالا: اول با دکمه‌ی tg://، اگر رد شد با لینک t.me/username، و اگر باز هم نشد بدون دکمه
ارسال می‌شود؛ پنل در هر حال می‌آید. مقدارهای خالی (`fox_points`) هم امن شدند.

## ۲) روباه با شکم پر تولید نمی‌کرد
ترتیب محاسبه غلط بود: اول گرسنگی کل زمان غیبت کم می‌شد، بعد تولید با شکمِ «کم‌شده»
حساب می‌شد. مثلاً روباه با شکم ۳/۳ اگر ۹۰ دقیقه پنل باز نمی‌شد، شکمش به ۱ می‌رسید و
تمام ۹۰ دقیقه‌ای که واقعاً سیر بود «صفر تولید» حساب می‌شد.
حالا تولید تا لحظه‌ای که شکم واقعاً به زیر ۲ می‌رسد حساب می‌شود، بعد گرسنگی اعمال می‌شود.

## نکته
وقتی ظرفیت انبار (📦) پر شود، تولید طبق طراحی تا «برداشت روب پوینت ها» متوقف می‌ماند
(پیام 🔴 ظرفیت پره در پنل روباه). این تغییر نکرده.

## دیپلوی
فقط `bot.py` را جایگزین کن؛ تغییر دیتابیس لازم نیست.
