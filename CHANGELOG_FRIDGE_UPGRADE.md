# ❄️ یخچال روبی — نمایش کامل، پخت و ارتقا

## ۱) نمایش یخچال (از سطح 7 کاربر باز می‌شود)
دستور `/یخچال`، پیام «یخچال روبی»، و دکمه «❄️ انداختن در یخچال روبی» بعد از شکار،
الان یخچال را دقیقاً با این قالب نشون می‌دن:

```
❄️ یخچال روبی <نام کاربر>

⭐️ سطح یخچال : <سطح فعلی> / 6

🦊 ظرفیت یخچال : <تعداد فعلی> / <ظرفیت>

〰️〰️〰️〰️〰️〰️〰️

🐇 خرگوش | خیلی کمیاب 🟣 | (خام)
┘─ ⚖️ وزن : 1.37 کیلو
┘─ 💰 ارزش : 7,000 🪙
┘─ 🍖 ارزش غذایی : 4

〰️〰️〰️〰️〰️〰️〰️

⭐ ارتقای بعدی یخچال : ...  (یا در سطح آخر: ✨ یخچال در آخرین سطح ممکن میباشد.)
```
زیر پیام، دکمه‌های شماره‌دار برای هر آیتم می‌آد؛ با زدن شماره‌ی هر آیتم، کارت
همون آیتم با دو گزینه‌ی «🔥 پختن» و «💰 فروختن» باز می‌شه.

## ۲) پختن شکار داخل یخچال
با زدن «🔥 پختن» روی یه آیتم خام، تایمر پخت شروع می‌شه:
- ارزش غذایی ۴ → ۶ دقیقه (۳۶۰ ثانیه)
- ارزش غذایی ۳ → ۴ دقیقه و ۳۰ ثانیه (۲۷۰ ثانیه)
- ارزش غذایی ۲ → ۴ دقیقه (۲۴۰ ثانیه)
- ارزش غذایی ۱ → ۳ دقیقه و ۵۰ ثانیه (۲۳۰ ثانیه)

وقتی پخت تموم بشه (خودکار، همین که کاربر دوباره یخچال/آیتم رو باز کنه محاسبه
می‌شه، نیازی به فرآیند پس‌زمینه نیست)، **ارزش غذایی دو برابر و ارزش فروش هم
دو برابر** می‌شه و وضعیت آیتم از «خام» به «پخته» تغییر می‌کنه. از اون به بعد
فقط گزینه‌ی «💰 فروختن» (با قیمت جدید) روی اون آیتم می‌مونه.

## ۳) ارتقای یخچال (۵ ارتقا)
دکمه‌ی «⭐ ارتقای یخچال» پایین لیست یخچال:
- ارتقای ۱ (رسیدن به سطح ۲): **رایگان** — ۱ جای اضافه
- ارتقای ۲ (سطح ۳): ۲۵۰,۰۰۰ روب‌پوینت
- ارتقای ۳ (سطح ۴): ۳۷۵,۰۰۰ روب‌پوینت
- ارتقای ۴ (سطح ۵): ۵۰۰,۰۰۰ روب‌پوینت
- ارتقای ۵ (سطح ۶، آخرین سطح): ۷۵۰,۰۰۰ روب‌پوینت

در سطح ۶ دیگه دکمه‌ی ارتقا نمی‌آد و پیام «✨ یخچال در آخرین سطح ممکن میباشد.»
نشون داده می‌شه.

## دیپلوی
سه ستون جدید اضافه شدن: `users.fridge_level`، `fox_hunts.weight`،
`fox_hunts.cooked`، `fox_hunts.cooking_started_at`. مثل بقیه‌ی ستون‌های قبلی،
`init_db()` خودش با `ALTER TABLE` این‌ها رو روی دیتابیس موجود اضافه می‌کنه؛
هیچ داده‌ای پاک نمی‌شه و هیچ migration دستی لازم نیست. همین فایل‌ها رو
جایگزین نسخه‌ی فعلی روی گیت‌هاب کن، Railway خودش دیپلوی جدید می‌سازه.

> فرض‌های من (چون تو درخواست ذکر نشده بودن):
> - **ظرفیت پایه‌ی یخچال** (قبل از هر ارتقا): 1 جا (یعنی همون اول که یخچال باز
>   می‌شه 1/1 نشون داده می‌شه)؛ هر ارتقا هم 1 جا اضافه می‌کنه تا سطح آخر برسه به 6/6.
> - **زمان پخت برای ارزش غذایی ۵** (فقط آهو) تو درخواست نبود؛ با همون الگوی
>   رشد بقیه‌ی سطوح (اختلاف هر پله ≈ ۳ برابر پله‌ی قبل: ۱۰، ۳۰، ۹۰ ثانیه) ادامه
>   دادم و رسیدم به ۶۳۰ ثانیه (۱۰ دقیقه و ۳۰ ثانیه).
> - **رده‌ی کمیابی و وزن هر حیوان**: تو درخواست فقط یه نمونه‌ی نمایشی
>   (کمیاب/۱.۳۷ کیلو) اومده بود، نه مقدار دقیق برای هر حیوون؛ برای هر حیوون یه
>   رده‌ی کمیابی (بر اساس ارزش غذایی خامش) و یه بازه‌ی وزن منطقی تعریف کردم که
>   هر بار شکار به‌صورت تصادفی از همون بازه رول می‌شه.
> - هر ارتقا (طبق تنها نمونه‌ی ذکرشده در درخواست، ارتقای اول) **۱ جای اضافه**
>   می‌ده؛ همین الگو رو برای هر ۵ ارتقا تکرار کردم.
