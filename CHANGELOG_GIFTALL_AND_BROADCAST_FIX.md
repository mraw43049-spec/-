# افزودن «هدیه روب‌پوینت به همه» + رفع باگ ارسال ناقص پیام همگانی

## ۱. هدیه‌ی روب‌پوینت رایگان به همه‌ی کاربرا (بخش جدید پشتیبانی)
توی پنل مدیریت (`/admin`) یک دکمه‌ی جدید اضافه شد: **«🎁 هدیه روب‌پوینت به همه»**.
- ادمین روی دکمه می‌زنه، یک عدد می‌فرسته (مثلاً `500`).
- همون مقدار به `fox_points` همه‌ی کاربرای ثبت‌شده اضافه می‌شه (اگه عدد منفی بفرسته، از همه کسر می‌شه؛
  موجودی هیچ‌وقت زیر صفر نمی‌ره).
- بعد از آپدیت دیتابیس، به همه‌ی کاربرا پیام اطلاع‌رسانی هدیه فرستاده می‌شه (با همون مکانیزم
  ضدفلود که در بخش بعد توضیح داده شده).

## ۲. چرا پیام همگانی فقط برای چند نفر می‌رفت؟
توی نسخه‌ی قبلی، پیام همگانی توی یک حلقه‌ی `for` بدون هیچ فاصله‌ای بین ارسال‌ها فرستاده می‌شد:

```python
for uid in users:
    try: await context.bot.send_message(uid, text); ok+=1
    except Exception: fail+=1
```

تلگرام محدودیت نرخ ارسال داره (تقریباً ۳۰ پیام در ثانیه، و برای پیام‌های همگانی حتی سخت‌گیرانه‌تر).
وقتی همه‌ی پیام‌ها پشت‌سرهم و بدون وقفه فرستاده می‌شدن، بعد از چند پیام اول تلگرام با خطای
**Flood control / Too Many Requests (RetryAfter)** بقیه‌ی درخواست‌ها رو رد می‌کرد. چون کد فقط
`except Exception: fail+=1` داشت، این خطاها بدون هیچ تلاش دوباره‌ای مستقیم «ناموفق» حساب
می‌شدن — یعنی فقط چند نفر اول پیام رو می‌گرفتن و بقیه اصلاً امتحان نمی‌شدن.

### راه‌حل
یک تابع مشترک `broadcast_to_users` اضافه شد که:
- بین ارسال هر پیام یک فاصله‌ی کوچیک (۵۰ میلی‌ثانیه) می‌ذاره تا از محدودیت نرخ تلگرام رد نشیم.
- اگه تلگرام خطای `RetryAfter` بده (یعنی «صبر کن و دوباره امتحان کن»)، به‌جای شکست فوری،
  دقیقاً همون مدت صبر می‌کنه و دوباره تلاش می‌کنه (حداکثر ۳ بار).
- فقط وقتی کاربر واقعاً بات رو بلاک کرده یا حسابش پاک شده (`Forbidden`/`BadRequest`) به‌عنوان
  «ناموفق» حساب می‌شه؛ خطاهای موقتی شبکه هم قبل از شکست یک بار دیگه امتحان می‌شن.

هم «پیام همگانی» و هم «هدیه روب‌پوینت به همه» الان از همین تابع استفاده می‌کنن، پس دیگه نباید
با تعداد زیاد کاربر، بیشترشون جا بمونن. فقط طول ارسال (به‌خاطر فاصله‌ی بین پیام‌ها) بیشتر می‌شه —
برای چند هزار کاربر ممکنه چند دقیقه طول بکشه که طبیعیه.
