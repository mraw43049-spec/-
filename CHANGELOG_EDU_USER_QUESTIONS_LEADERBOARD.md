# طرح سؤال توسط کاربر + لیدربرد «بیشترین پاسخ درست درس»

## لیدربرد جهانی
بخش جدید «🎓 بیشترین پاسخ درست درس»: ۱۰۰ نفر برتر بر اساس تعداد کل پاسخ‌های درست در روباهیو درس
(همان عددِ «🎓 پاسخ‌های درست» پروفایل درس). فقط کسانی که حداقل یک پاسخ درست دارند می‌آیند.

## طرح سؤال (داخل پنل «روباهیو درس» → «✍️ طرح سوال»)
1. موضوع را انتخاب می‌کند (کنار هر موضوع باقی‌ماندهٔ سهمیهٔ امروز نوشته می‌شود).
2. متن سؤال (۵ تا ۲۰۰ کاراکتر) ← گزینهٔ درست ← نادرست اول ← نادرست دوم (هر کدام تا ۴۰ کاراکتر،
   بدون تکرار). همه‌ی مرحله‌ها روی همان پیام پنل ویرایش می‌شوند و دکمه‌ی «❌ لغو» دارند.
3. پیش‌نمایش ← «✅ ارسال برای پشتیبانی».
- سقف: هر کاربر روزی ۳ سؤال برای هر موضوع (روز به وقت ایران؛ همهٔ سؤال‌های ارسالی حساب می‌شوند).

## پشتیبانی
سؤال با دکمه‌های «✅ تأیید (+۱٬۵۰۰)» و «❌ رد» برای همه‌ی ادمین‌ها (ADMIN_IDS) ارسال می‌شود.
- تأیید: ۱٬۵۰۰ روب‌پوینت به طراح اضافه می‌شود، سؤال وارد چرخهٔ همان موضوع می‌شود و به طراح پیام می‌رود.
- رد: بدون جایزه، به طراح خبر داده می‌شود.
- بعد از تصمیم، کارت همه‌ی ادمین‌ها به‌روز می‌شود و بررسی دوباره ممکن نیست.

## چرخهٔ درس
- سؤال‌های تأییدشده کنار سؤال‌های قبلی هر موضوع پرسیده می‌شوند و بالای سؤال «✍️ طراح: <اسم (لینک آبی)>» دیده می‌شود.
- ترتیب گزینه‌های سؤال کاربر هر بار بُر می‌خورد.
- سؤال‌های خودِ طراح به خودش داده نمی‌شود (تا با جواب دادن به سؤال خودش روی لیدربرد بالا نرود).
- وقتی همهٔ سؤال‌های یک موضوع دیده شد، دور از اول شروع می‌شود (قبلاً ریست ذخیره نمی‌شد).

## دیپلوی
فایل‌های `bot.py` و `education.py` جایگزین شوند. جدول جدید `edu_user_questions` خودکار ساخته می‌شود.
