# ⚽ پیش‌بینی فوتبال (جدید)

## ۱) پنل پشتیبانی (ادمین) — `/admin`
یک دکمه‌ی جدید «⚽ پیش‌بینی فوتبال» به پنل مدیریت اضافه شد که وارد یک زیرپنل
می‌شه با سه بخش:

- **➕ افزودن بازی** — پشتیبانی سه خط می‌فرسته: نام تیم میزبان، نام تیم مهمان،
  و تاریخ/ساعت بازی (متن آزاد، هر فرمتی که بخواد). بازی بلافاصله برای
  پیش‌بینی کاربرا باز می‌شه.
- **📋 لیست بازی‌ها** — همه‌ی بازی‌های ثبت‌شده رو نشون می‌ده؛ جلوی هرکدوم دو
  دکمه هست: «🔁 تغییر وضعیت» (باز/بسته کردن بازی برای پیش‌بینی، بدون حذفش) و
  «🗑 حذف» (حذف کامل بازی؛ پیش‌بینی‌های ثبت‌شده روی اون بازی هم حذف می‌شن).
- **🗳 پیش‌بینی‌های در انتظار تایید** — لیست پیش‌بینی‌های کاربرا که هنوز بررسی
  نشدن؛ جلوی هرکدوم «✅ تایید» و «❌ رد» هست.
  - با تایید: **1500 روب‌پوینت** به حساب کاربر اضافه می‌شه و توی پیوی خبردار
    می‌شه.
  - با رد: چیزی کسر/اضافه نمی‌شه، فقط توی پیوی به کاربر خبر داده می‌شه.

## ۲) سمت کاربر — نوشتن «پیش بینی»
هر کاربری (تو گروه یا پیوی) با نوشتن **«پیش بینی»** (یا «پیش‌بینی»،
«پیشبینی»، «پیش بینی فوتبال») لیست بازی‌های **باز** رو به‌صورت دکمه می‌بینه.
با زدن روی یه بازی، سه گزینه نشون داده می‌شه:
- 🏠 برد میزبان
- 🤝 مساوی
- 🚩 برد میهمان

با زدن روی هرکدوم، پیش‌بینی کاربر ثبت می‌شه و پیام «⏳ منتظر تایید پشتیبانی
باش» رو می‌بینه. هر کاربر برای هر بازی فقط **یک بار** می‌تونه پیش‌بینی کنه
(دومی رد می‌شه). بعد از اینکه پشتیبانی تایید یا رد کرد، کاربر توی پیوی
اطلاعیه می‌گیره.

## ۳) دیتابیس
دو جدول جدید اضافه شد (جدول‌های قبلی دست‌نخورده می‌مونن):
- `football_matches`: `id, team_home, team_away, match_time, status
  (open/closed), created_by, created_at`
- `football_predictions`: `id, match_id, user_id, choice
  (home/draw/away), status (pending/approved/rejected), reward,
  created_at, reviewed_at`

چون این دو جدول کاملاً جدیدن (نه ستون جدید روی جدول موجود)، با همون
`Base.metadata.create_all` که همیشه موقع استارت اجرا می‌شه خودکار ساخته
می‌شن؛ نیازی به ALTER TABLE دستی نیست و هیچ داده‌ی قبلی (کاربرا، روب‌پوینت،
شهر و ...) پاک نمی‌شه.

## دیپلوی
همین فایل‌ها رو جایگزین نسخه‌ی فعلی روی گیت‌هاب کن؛ Railway خودش دیپلوی جدید
می‌سازه و جدول‌های جدید خودکار به دیتابیس اضافه می‌شن.

> فرض‌های من (چون تو درخواست ذکر نشده بودن):
> - فرمت پیش‌بینی رو «۱X۲» (برد میزبان / مساوی / برد میهمان) گذاشتم، نه
>   پیش‌بینی نتیجه‌ی دقیق (مثلاً ۲-۱)، چون ساده‌تر و سریع‌تره هم برای کاربر
>   هم برای بررسی پشتیبانی.
> - «ساعت بازی» رو به‌صورت متن آزاد ذخیره کردم (نه تاریخ واقعی)، چون فقط
>   برای نمایشه و پشتیبانی دستی واردش می‌کنه؛ همینطوری پشتیبانی می‌تونه هر
>   فرمت تاریخی (شمسی/میلادی) که خواست بنویسه.
> - علاوه بر افزودن/حذف بازی، یه دکمه‌ی «باز/بسته کردن» هم اضافه کردم تا
>   پشتیبانی بتونه بدون حذف کامل بازی، جلوی پیش‌بینی جدید (مثلاً بعد از شروع
>   بازی) رو بگیره.
> - جایزه‌ی تایید (1500 روب‌پوینت) ثابته و فقط پشتیبانی می‌تونه با دکمه‌ی
>   تایید بدتش؛ رد کردن جایزه‌ای نداره.
