# تغییرات این نسخه

## ۱. فروشگاه گیفت — انتخاب طرح
بعد از انتخاب تعرفه (15 / 25 / 50 استارزی)، حالا یک مرحله جدید اضافه شده که کاربر باید طرح گیفت رو انتخاب کنه:

- **15 استارزی**: 🧸 تدی  |  💝 قلب
- **25 استارزی**: 🎁 کادو  |  🌹 گل رز
- **50 استارزی**: 🎂 کیک  |  💐 دسته گل  |  🚀 سفینه  |  🍾 شامپاین

مسیر: فروشگاه → انتخاب تعرفه → انتخاب طرح → تعداد → آیدی گیرنده → متن گیفت → رسید.

طرح انتخاب‌شده در دیتابیس (`gift_orders.gift_design`) و در پیام‌های ادمین/کانال هم نمایش داده می‌شه.

## ۲. گزینه «بدون متن»
تو مرحله‌ای که از کاربر متن گیفت خواسته می‌شه، یک دکمه شیشه‌ای «🚫 بدون متن» هم اضافه شده. با زدنش،
متن گیفت خالی ثبت می‌شه و مستقیم می‌ره به مرحله خلاصه سفارش/رسید.

## ۳. محرومیت کاربر (پنل پشتیبانی/ادمین)
تو `/admin` یک دکمه جدید «🚫 محرومیت کاربر» اضافه شده که زیرمنوی زیر رو باز می‌کنه:

- ⏱ ۱ روزه
- ⏱ ۷ روزه
- ⏱ ۳۰ روزه
- ⛔ دائم
- ✅ رفع محرومیت

بعد از انتخاب مدت، ادمین فقط آیدی عددی کاربر رو می‌فرسته. کاربر محروم:

- برای محرومیت موقت (۱/۷/۳۰ روز): فیلد `banned_until` در دیتابیس ست می‌شه و بعد از اتمام مدت، خودکار
  (در همون لحظه‌ای که کاربر دوباره با ربات کار کنه) رفع می‌شه.
- برای محرومیت دائم: از همون فیلد قبلی `is_banned` استفاده می‌شه (سازگار با بن خودکار قبلیِ فروشگاه گیفت).
- «رفع محرومیت» هر دو حالت (دائم/موقت) رو پاک می‌کنه.

در تمام حالت‌ها، `ban_gate` جلوی هر نوع استفاده کاربر از ربات (پیام، دستور، دکمه) رو می‌گیره تا وقتی که
پشتیبانی محرومیتش رو برداره یا مدتش تموم بشه. ادمین‌ها (`ADMIN_IDS`) از این محدودیت مستثنا هستن.

کاربر هم موقع محروم/رفع‌محروم شدن یک پیام اطلاع‌رسانی خصوصی دریافت می‌کنه (در صورتی که قبلاً ربات رو
استارت زده باشه).

## دیتابیس
- ستون جدید `users.banned_until` (nullable, برای بن موقت) — به‌صورت خودکار به دیتابیس قدیمی هم اضافه می‌شه (migration).
- ستون جدید `gift_orders.gift_design` (nullable) — برای ذخیره طرح انتخابیِ گیفت.
