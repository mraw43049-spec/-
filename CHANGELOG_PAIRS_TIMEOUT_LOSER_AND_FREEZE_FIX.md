# دوتایی‌ها: پایان نوبت = باخت + رفع گیر کردن میز

## ⏰ نوبت تموم‌شده = باخت
- قبلاً اگه کسی سر نوبتش (۶۰ ثانیه) حرکت نمی‌زد، فقط نوبت به نفر بعدی می‌رفت و بازی ادامه پیدا می‌کرد.
- حالا دقیقاً مثل بازی‌های دیگه‌ی روبی (دوز، خرگوش خور)، اگه کسی تو ۶۰ ثانیه حرکت نکنه، بازنده‌ی بازی اعلام می‌شه، حریف برنده می‌شه و کل جایزه‌ی میز (در صورت وجود) رو می‌گیره. بازی همون‌جا تموم می‌شه.

## 🃏 رفع گیر کردن گاه‌به‌گاه میز دوتایی‌ها
- زمان نمایش دو کارتِ جفت‌نشده از ۰.۱۵ ثانیه (تقریباً نامرئی) به ۱.۳ ثانیه رسید. ۰.۱۵ ثانیه باعث می‌شد دو تا ویرایش پیام (ویرایش نمایش کارت‌ها + ویرایش برگردوندن نوبت) پشت‌سرهم و خیلی نزدیک به هم به تلگرام ارسال بشه؛ تلگرام گاهی ویرایش دوم رو رد می‌کرد (محدودیت نرخ درخواست) و پیام با وضعیت قدیمی می‌موند — همون «گیر کردن میز».
- یه باگ دیگه هم بود: با هر بار «باز کردن کارت اول» (حتی وقتی نوبت عوض نشده بود) یه تایمر و رفرش تکراری با همون توکن قبلی ساخته می‌شد. این تایمرهای تکراری روی هم جمع می‌شدن و باعث می‌شدن گاهی نوبت بی‌دلیل یا زودتر از موقع رد بشه. حالا فقط وقتی نوبت واقعاً عوض می‌شه (جفت پیدا شد) تایمر جدید ساخته می‌شه.
- اگه دقیقاً همون لحظه‌ای که دو کارتِ جفت‌نشده داره نمایش داده می‌شه (۱.۳ ثانیه) تایمر نوبت هم برسه، دیگه گم نمی‌شه؛ کمی بعد دوباره چک می‌کنه به‌جای اینکه کامل نادیده گرفته بشه.

فایل تغییر: `bot.py` (توابع `pairs_turn_timeout`, `ruby_pairs_move`, ثابت `PAIRS_MISMATCH_REVEAL_SECONDS`).
