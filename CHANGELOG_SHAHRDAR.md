# 🦁 انتخابات شهرداری (جدید) + سطح شهر با عدد انگلیسی

## ۱) سطح شهر دیگه رومی نیست
- قبلاً سطح شهر با عدد رومی (I تا XI) نشون داده می‌شد؛ الان همه‌جا (پنل شهر،
  پیام ارتقا، پیام تشکر از دونیت) با عدد معمولی انگلیسی نشون داده می‌شه:
  مثلاً «⭐️ سطح شهر : 5».

## ۲) انتخابات شهرداری — از سطح شهر 5 به بعد
- تو هر گپی که شهرش به **سطح 5** رسیده باشه، کاربرا با نوشتن **«شهردار روبی»**
  (یا `/شهردار`، `/شهردار روبی`) پنل انتخابات رو می‌بینن.
- اگه شهرداری در جریان نباشه (یا دوره‌ی شهردار قبلی تموم شده باشه)، همون لحظه
  یه دور جدید ثبت‌نام کاندید خودکار باز می‌شه.

### مرحله‌ی ثبت‌نام کاندید
- هر کسی می‌تونه با زدن دکمه‌ی **🙋 کاندید شدن**، با هزینه‌ی **10,000 روب‌پوینت**
  کاندید بشه.
- ظرفیت کاندید **5 نفر** است. همین که نفر پنجم ثبت‌نام کنه، رای‌گیری فوراً شروع
  می‌شه (نیازی به منتظر موندن تا آخر مهلت نیست).
- اگه ظرفیت پر نشه، **حداکثر تا 24 ساعت** بعد از باز شدن ثبت‌نام، رای‌گیری با
  همون کاندیدهایی که ثبت‌نام کردن (حتی اگه فقط 1 نفر باشه) خودکار شروع می‌شه.
- اگه هیچ‌کس ظرف این مدت کاندید نشه، انتخابات بسته می‌شه و دفعه‌ی بعد که
  «شهردار روبی» زده بشه، ثبت‌نام دوباره باز می‌شه.

### مرحله‌ی رای‌گیری
- بقیه‌ی کاربرا (و خود کاندیدها) می‌تونن روی دکمه‌ی هرکدوم از کاندیدها بزنن تا
  بهش رای بدن. **هر کاربر فقط یک بار** می‌تونه رای بده (رای دوم رد می‌شه).
- رای‌گیری **حداکثر 5 ساعت** طول می‌کشه.
- در پایان (یا وقتی زمان تموم بشه)، کاندیدی که **بیشترین رای** رو آورده باشه
  شهردار می‌شه؛ تو گپ اعلام عمومی می‌شه و خود برنده هم تو پیوی خبردار می‌شه
  (در تساوی، کاندیدی که زودتر ثبت‌نام کرده بود برنده می‌شه).

### دوره‌ی شهرداری
- شهردار منتخب **3 روز** سر کار می‌مونه. تا وقتی دوره‌ش تموم نشده، «شهردار روبی»
  فقط اطلاعات شهردار فعلی و زمان باقی‌مونده رو نشون می‌ده.
- بعد از تموم شدن 3 روز، دفعه‌ی بعدی که کسی «شهردار روبی» رو بزنه، دور جدید
  انتخابات (ثبت‌نام کاندید) خودش باز می‌شه — یعنی هر 3 روز یک‌بار شهردار عوض
  می‌شه.
- تا وقتی هیچ‌کس تو یه گپ انتخاب نشده، پنل «شهر روبی» مثل قبل مالک گپ (ادمین
  creator) رو به‌عنوان شهردار نشون می‌ده؛ همین که یه شهردار با رای مردم انتخاب
  بشه، پنل «شهر روبی» هم به‌جاش «(منتخب مردم 🗳)» رو نشون می‌ده.

## ۳) دیتابیس
- 8 ستون جدید روی جدول موجود `group_chats` اضافه شد (نه جدول جدید):
  `city_mayor_id`, `city_mayor_name`, `city_mayor_term_ends_at`,
  `city_election_status`, `city_election_candidates`, `city_election_votes`,
  `city_election_candidacy_ends_at`, `city_election_voting_ends_at`.
- مثل بقیه‌ی migration های پروژه، با `ALTER TABLE` امن اضافه می‌شن؛ هیچ داده‌ی
  قبلی (روب‌پوینت، شهر، دونیت‌ها و ...) پاک نمی‌شه.

## دیپلوی
همین فایل‌ها رو جایگزین نسخه‌ی فعلی روی گیت‌هاب کن؛ Railway خودش دیپلوی جدید
می‌سازه و ستون‌های جدید خودکار به دیتابیس اضافه می‌شن (نیاز به کار دستی رو
دیتابیس نیست).

> فرض‌های من (چون تو درخواست ذکر نشده بودن):
> - مهلت ثبت‌نام کاندید قبل از شروع خودکار رای‌گیری رو 24 ساعت گذاشتم (اگه
>   ظرفیت 5 نفر زودتر پر بشه، رای‌گیری زودتر از اون هم شروع می‌شه).
> - اگه رای‌گیری با تساوی تموم بشه، کاندیدی که زودتر ثبت‌نام کرده بود شهردار
>   می‌شه.
> - خود کاندیدها هم می‌تونن به کاندیدها (از جمله خودشون) رای بدن؛ محدودیتی
>   روی این نبود.
