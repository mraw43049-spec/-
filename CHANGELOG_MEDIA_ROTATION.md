# پخش نوبتی و بدون تکرار آهنگ / عکس / ویدیو

## مشکل
انتخاب آهنگ و مدیا تصادفی بود (`random.choice`)؛ برای آهنگ‌های «روباهیو حال» فقط
«آهنگ قبلیِ همان کاربر» حذف می‌شد و آن هم در حافظه بود و با هر دیپلوی پاک می‌شد.
برای عکس/ویدیو/گیف/استیکرِ یادگرفته‌شده هم هیچ جلوگیری از تکرار نبود.

## حالا
- ترتیب پخش نوبتی است (به ترتیبِ اضافه شدن آیتم‌ها)، نه تصادفی.
- تا همه‌ی آیتم‌های یک گروه یک‌بار پخش نشدن، هیچ‌کدام تکراری نمی‌آید؛ بعد از پخش شدنِ همه،
  دور از اولین آیتم دوباره شروع می‌شود.
- «گروه» یعنی: برای «روباهیو حال» هر حال (شاد، آروم، ...)، و برای مدیای یادگرفته‌شده
  هر کلید (مثلاً همه‌ی عکس‌هایی که با کلید «عکس» یاد داده شده).
- وضعیت در دیتابیس (جدول جدید `media_rotation`) ذخیره می‌شود، پس با ری‌استارت هم از دست نمی‌رود.
- اگر وسط دور آیتم جدیدی اضافه شود، همان دور پخش می‌شود؛ آیتم حذف‌شده مشکلی ایجاد نمی‌کند.
- نوبت برای همه‌ی کاربران مشترک است (هر درخواست، آیتم بعدی را می‌دهد).
- جواب‌های متنی (مثلاً جوک‌ها) دست نخورده‌اند و هنوز تصادفی‌اند.

## دیپلوی
سه فایل `bot.py`، `fox_brain.py` و `database.py` را جایگزین کن. جدول جدید خودکار ساخته می‌شود.
