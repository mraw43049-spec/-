# ساخت کد هدیه ۴گزینه‌ای + اسم‌های آبی و کلیک‌پذیر

## ۱) ساخت کد هدیه (پنل ادمین)
با زدن «🎟 ساخت کد هدیه» پنلی با ۴ گزینه باز می‌شود؛ هر گزینه را بزن و تنظیم کن:
- 🔤 **فرمت کد**: تصادفی (حروف+عدد) / تصادفی فقط عدد / تصادفی فقط حروف / تصادفی با پیشوند دلخواه (مثل `FOX-K7M2QX`) / کد دلخواه
- 👥 **حداکثر افراد**: ۱ / ۱۰ / ۵۰ / ۱۰۰ / ۵۰۰ یا عدد دلخواه
- 💰 **جایزه روب‌پوینت**: چند مقدار آماده یا عدد دلخواه (مثلاً `5000` یا `5k`)
- ⏳ **زمان**: ۱ ساعت / ۶ ساعت / ۲۴ ساعت / ۷ روز / ۳۰ روز / بدون محدودیت / دلخواه (ساعت)

کنار هر گزینه‌ای که تنظیم شد ✅ می‌آید و وقتی هر ۴ مورد کامل شد دکمه‌ی «🎟 ساخت کد» ظاهر می‌شود. کد ساخته‌شده با یک لمس قابل کپی است.

- کد تصادفی همیشه یکتا است (تکراری ساخته نمی‌شود) و حروف/اعداد شبیه هم (O/0، I/1) ندارد.
- زمانِ اعتبار از لحظه‌ی ساخت شروع می‌شود. کاربری که بعد از انقضا کد را بفرستد پیام «مهلت استفاده تمام شده» می‌گیرد.
- دیتابیس: ستون جدید `gift_codes.expires_at` (خودکار migrate می‌شود؛ کدهای قدیمی بدون انقضا می‌مانند).
- باگ کوچک هم رفع شد: پیام «کد هدیه با موفقیت ثبت شد» بعد از بستن session خوانده می‌شد (`DetachedInstanceError`) و ممکن بود جایزه داده شود ولی پیام تأیید نرسد.

## ۲) اسم کاربران در همه‌ی بخش‌ها آبی و کلیک‌پذیر
هر جا اسم کاربر داخل «متن» پیام می‌آید (کازینو و همه‌ی میزهای بازی، لیدربرد، شهر/شهردار/دونیت، بانک، کارت‌به‌کارت، انتقال، کارخانه، یخچال، پروفایل، دوستان، روباه زخمی، رفرال، آمار، چالش، لیست پیش‌بینی ادمین) اسم به‌صورت لینک آبی نمایش داده می‌شود و با لمس آن پروفایل تلگرامی همان کاربر باز می‌شود.

روش فنی: `user_mention(user)` / `mention_of(id, name)` دور اسم نشانه می‌گذارند و یک wrapper روی `ExtBot` (`send_message`، `edit_message_text`، کپشن‌ها، …) قبل از ارسال، آن‌ها را به entity از نوع `text_mention` تبدیل می‌کند. بنابراین هیچ `parse_mode` عمومی وجود ندارد و متن‌های دیگر (مثلاً پیام کاربران) دست‌نخورده می‌مانند.
- اگر تلگرام یک entity را رد کند، همان پیام بدون لینک ارسال می‌شود (پیام هیچ‌وقت گم نمی‌شود).
- متن دکمه‌ها و alert ها (پاپ‌آپ) طبق محدودیت تلگرام نمی‌توانند رنگی/لینک باشند و ساده می‌مانند؛ دکمه‌های «👤 پروفایل» قبلی هم سر جایشان هستند.
- برای بخش‌های جدید فقط از `user_mention(u)` داخل متن پیام استفاده کن (برای دکمه‌ها `user_display_name(u)`).
