# هوش مصنوعی روباهیو
فایل جدید `ai_service.py` (کلاینت API، سقف مصرف، حافظه‌ی گفتگو، پرامپت‌ها) + هندلرهای هوش مصنوعی در `bot.py` + ستون `group_chats.ai_mod` در `database.py` (خودکار migrate می‌شود).
جزئیات راه‌اندازی: `AI_SETUP.md`.

- چت با روباه (پیوی، «روباهیو …»، منشن، ریپلای)، «راهنما <سوال>»، ناظر هوشمند گروه (opt-in)، «اخبار شهر» + خبر خودکار ارتقای شهر.
- همه‌ی تماس‌های API در پس‌زمینه (`application.create_task`) اجرا می‌شوند تا ربات منتظر جواب نماند.
- بدون `AI_API_KEY` همه‌چیز بی‌صدا غیرفعال است و بقیه‌ی ربات بدون تغییر کار می‌کند.
- ابزار ادمین (فقط ADMIN_IDS، پیوی): «وضعیت هوش مصنوعی» و «تست هوش مصنوعی».
- بخش‌های جدید در «راهنمای کامل» درون ربات اضافه می‌شوند (فقط وقتی هوش مصنوعی فعال است).
