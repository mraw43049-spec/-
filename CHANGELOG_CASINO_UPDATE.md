# به‌روزرسانی کازینو روبی

## 🎰 گردونه شانس (cz_wheel)
- امتیاز لازم برای گرفتن جایزه از ۵۷ به **بالای ۵۰** کاهش پیدا کرد.
- ضریب ۲.۷× حالا فقط مخصوص جکپات کامل **7️⃣7️⃣7️⃣** است؛ بقیه‌ی نتایج بالای ۵۰ امتیاز ضریب ۱.۵× می‌گیرند (`WHEEL_WIN_MULTIPLIER` در بالای `bot.py`).
- جدول تبدیل امتیاز دیگر به کاربر نشان داده نمی‌شود؛ فقط ترکیب و نتیجه (برد/باخت) نمایش داده می‌شود.

## 🎯 ایموجی اضافه در پنل میزها
- ایموجی 🎯 که به‌اشتباه در پیام «روی همین پیام ریپلای کن...» برای همه‌ی بازی‌ها (حتی تاس و گردونه) نشان داده می‌شد، حذف شد.

## 🎲 تاس کازینو (cz_dice) — شرط‌بندی جدید
- **دو نفره:** سازنده‌ی میز از بین ۴ گزینه یکی را انتخاب می‌کند: 🔢 فرد / 🔢 زوج / ⬆️ بالاترین تاس / ⬇️ پایین‌ترین تاس. به‌محض انتخاب، گزینه‌ی مقابل (فرد↔زوج یا بالاترین↔پایین‌ترین) خودکار برای حریف تنظیم می‌شود.
  - شرط فرد/زوج بر اساس **مجموع دو تاس** بررسی می‌شود.
  - شرط بالاترین/پایین‌ترین بر اساس مقایسه‌ی مستقیم دو تاس؛ در صورت تساوی، مبلغ ورودی به هر دو نفر برگردانده می‌شود.
- **تکی:** فقط دو گزینه فرد/زوج قابل انتخاب است؛ در صورت برد، ۱.۹× مبلغ ورودی به‌عنوان جایزه تعلق می‌گیرد (`DICE_SOLO_WIN_MULTIPLIER`).

> ضرایب بالا (۱.۵×, ۱.۹×) پیش‌فرض معقول هستند چون در درخواست دقیق مشخص نشده بودند؛ در صورت نیاز به تغییر، همین دو ثابت در ابتدای بخش کازینوی `bot.py` را ویرایش کنید.

## 🤒 مریضی روباه (جدید)
- از **لول ۶** به بعد، هر **۴۸ ساعت** یک‌بار روباه به‌طور تصادفی مریض می‌شود (دلیل تصادفی از یک لیست کوچک انتخاب می‌شود).
- تا وقتی درمان نشه، هیچ‌کدوم از این دستورها کار نمی‌کنن و فقط پیام مریضی + ۳ گزینه‌ی درمان رو نشون می‌دن: روباه، شکار، یخچال، بانک، بازی‌های روبی، کازینو روبی، گردونه، روب‌روب.
- سه گزینه‌ی درمان (فقط یکی رو می‌تونی شروع کنی؛ تا آخر همون رو ادامه بده):
  - 💊 **قرص** — ۵٬۰۰۰ روب‌پوینت هر بار، هر ۳ دقیقه یک‌بار، ۳ بار پشت‌سرهم.
  - 🧴 **شربت** — ۱۰٬۰۰۰ روب‌پوینت هر بار، هر ۱ دقیقه یک‌بار، ۳ بار پشت‌سرهم.
  - 🛌 **استراحت** — رایگان؛ روباه ۱ ساعت می‌خوابه و خودش خوب می‌شه (نیازی به کار دیگه نیست).
- فیلدهای جدید دیتابیس (`fox_sick_since`, `fox_sick_reason`, `fox_sick_treatment`, `fox_sick_doses_given`, `fox_sick_next_dose_at`, `fox_sick_rest_until`, `fox_last_sick_at`) با همون سیستم Migration خودکار موجود اضافه می‌شن؛ داده‌های قبلی پاک نمی‌شن.

