# چرا روب‌پوینت‌ها بعضی وقتا ریست می‌شه؟ (و راه حل قطعی)

## علت اصلی
ربات به‌صورت پیش‌فرض از یه فایل SQLite (`bot.db`) روی همون سرور استفاده می‌کنه.
روی Railway، هر بار که کد جدید دیپلوی می‌شه (یعنی هر بار فایل‌های گیت‌هاب رو
عوض می‌کنی)، یه کانتینر کاملاً تازه ساخته می‌شه و هر چی روی دیسک کانتینر قبلی
بوده (از جمله `bot.db` با همه‌ی روب‌پوینت‌ها و لول‌ها) از بین می‌ره — مگر
اینکه دیتابیس رو جای پایدار (Persistent) نگه داری.

دو تا چیز رو تو این آپدیت درست کردیم:
1. `bot.db` رو تو `.gitignore` و `.dockerignore` گذاشتیم تا اگه یه نسخه‌ی
   قدیمی/خالی از این فایل تو ریپوی گیت‌هابت باشه، اصلاً وارد ایمیج دیپلوی
   نشه و جای دیتابیس زنده رو نگیره.
2. یه بکاپ خودکار روزانه (و یه دکمه‌ی بکاپ دستی تو پنل ادمین) اضافه کردیم که
   فایل JSON از همه‌ی کاربرا برات می‌فرسته؛ این یه شبکه‌ی ایمنیه، نه جایگزین
   دیتابیس پایدار.

اما این‌ها کافی نیست؛ **راه‌حل قطعی اینه که دیتابیس رو از حالت فایل محلی
دربیاری.** دو گزینه داری:

## گزینه ۱ (پیشنهاد من): استفاده از PostgreSQL خود Railway
1. تو داشبورد Railway، داخل همون پروژه، دکمه‌ی **New** رو بزن و
   **Database → PostgreSQL** رو انتخاب کن. یه سرویس Postgres جدا از سرویس
   ربات ساخته می‌شه که کاملاً پایداره و با دیپلوی مجدد ربات کاری بهش نداره.
2. وارد سرویس Postgres شو، تب **Variables** رو باز کن و مقدار
   `DATABASE_URL` رو کپی کن (یا از تب Connect بگیر).
3. برو تو سرویس ربات (worker) → **Variables** → یه متغیر جدید بساز به اسم
   `DATABASE_URL` و همون مقداری که کپی کردی رو بذار.
4. ربات رو دوباره دیپلوی کن. کد از قبل آماده‌ست و خودش تشخیص می‌ده که
   Postgres هست (نیازی به تغییر کد نیست). لاگ استارتاپ هم می‌نویسه
   `Database in use: PostgreSQL (پایدار ✅)`.
5. از این به بعد، هر چقدر هم فایل‌های گیت‌هاب رو عوض کنی و دیپلوی کنی،
   داده‌های کاربرا (روب‌پوینت، لول، بانک، ...) دست‌نخورده می‌مونه، چون
   دیتابیس یه سرویس کاملاً جدا و پایداره.

## گزینه ۲: نگه داشتن SQLite ولی با یه Volume پایدار
اگه نمی‌خوای Postgres اضافه کنی:
1. تو سرویس ربات روی Railway، برو تب **Settings → Volumes** و یه Volume
   جدید بساز (مثلاً با mount path: `/data`).
2. تو Variables سرویس ربات، `DATABASE_URL` رو بذار روی:
   `sqlite:////data/bot.db`
3. دیپلوی کن. حالا فایل دیتابیس بیرون از کانتینر (روی Volume) ذخیره می‌شه و
   با هر دیپلوی جدید پاک نمی‌شه.

> ⚠️ گزینه ۱ مطمئن‌تره؛ Volume برای SQLite هم کار می‌کنه ولی برای بازی‌های
> هم‌زمان و چند کاربره، Postgres پایدارتر و امن‌تره.

## چک کردن اینکه درست تنظیم شده
بعد از دیپلوی، تو لاگ‌های Railway دنبال این خط بگرد:
```
Database in use: PostgreSQL (پایدار ✅)
```
اگه به‌جاش نوشته بود «SQLite محلی (⚠️ ...)» یعنی هنوز `DATABASE_URL` درست
تنظیم نشده و هنوز در معرض خطر ریست شدنی.

## شبکه‌ی ایمنی اضافه (بکاپ)
- هر روز یه بار، فایل JSON از همه‌ی کاربرا (روب‌پوینت، لول، حساب بانکی) به
  آیدی عددی همه‌ی `ADMIN_IDS` فرستاده می‌شه (به‌شرطی که قبلاً ربات رو
  پی‌وی استارت زده باشن).
- از پنل `/admin` هم دکمه‌ی «📦 دریافت بکاپ اطلاعات» هست که همین فایل رو
  فوری می‌فرسته.
- این فایل فقط برای بازیابی دستیه (یه انسان باید دستی مقادیرش رو با دستور
  ادمین برگردونه)؛ جای دیتابیس اصلی رو نمی‌گیره.
