# 🍖 رفع باگ «شکم روباه خالی نمی‌شه»

## مشکل
عدد گرسنگی (`FOX_HUNGER_INTERVAL_SECONDS = 25 دقیقه`) و فیلد زمانش
(`fox_last_hunger_at`) از قبل تو کد بودن، ولی هیچ‌جا واقعاً استفاده نمی‌شدن؛
هیچ تابعی وجود نداشت که با گذر زمان شکم روباه رو کم کنه. برای همین شکم فقط
با «دادن شکار به روباه» بالا می‌رفت و هیچ‌وقت خودش خالی نمی‌شد.

## راه‌حل
تابع `settle_fox_hunger()` اضافه شد و داخل `update_fox_production()` (یعنی
همون‌جایی که هر بار پروفایل روباه/برداشت/دادن شکار خونده یا محاسبه می‌شه)
صدا زده می‌شه. رفتارش:
- هر **۲۵ دقیقه** دقیقاً **۱ واحد** از شکم کم می‌شه.
- این کار **مداوم و بی‌پایان** ادامه داره (نه فقط یه بار) — هر چند بار هم
  کاربر سر نزنه، دفعه‌ی بعد که پروفایل روباه باز بشه همه‌ی بازه‌های ۲۵
  دقیقه‌ایِ سپری‌شده یکجا محاسبه و کم می‌شن.
- شکم زیر صفر نمی‌ره.
- باقیمانده‌ی زمانِ ناقص (کمتر از ۲۵ دقیقه) از دست نمی‌ره؛ دقیقاً مثل الگوی
  تولید روب‌پوینت روباه محاسبه می‌شه.

## دیپلوی
هیچ ستون یا migration جدیدی لازم نداره؛ `fox_last_hunger_at` از قبل تو
دیتابیس بود. فقط کافیه فایل‌ها رو جایگزین کنی و دیپلوی جدید بزنی.
