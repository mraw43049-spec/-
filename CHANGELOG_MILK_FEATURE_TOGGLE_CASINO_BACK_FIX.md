# تغییرات این نسخه

## ۱. شیر دادن نینی روبی رایگان شد
- دیگه هزینه‌ای بابت شیر دادن کم نمی‌شه؛ فقط محدودیت زمانی (هر ۳ ساعت یک‌بار) همچنان پابرجاست.
- دکمه‌ی «شیر دادن» حالا «رایگان» رو نشون می‌ده به‌جای مبلغ.

## ۲. غیرفعال‌سازی بخش‌های ربات توسط پشتیبانی (جدید)
- پشتیبانی (آیدی‌های داخل ADMIN_IDS) الان می‌تونه توی هر گپی که ربات توشه با نوشتن
  «**<اسم بخش> غیرفعال**» اون بخش رو فقط برای همون گپ خاموش کنه، و با
  «**<اسم بخش> فعال**» دوباره روشنش کنه.
- این قابلیت مخصوص «روباه زخمی» نیست؛ روی همه‌ی بخش‌های اصلی ربات کار می‌کنه:
  روباه زخمی، کازینو، بازی روبی، ازدواج روبی، نینی روبی، روباهیو درس، شکار،
  قاچاق روبی، زندان روبی، مارکت روبی، شهر روبی، شهردار روبی، بانک روبی، گردونه،
  رفرال، لیدر برد، دوست روبی، شاپ روبی، کد هدیه، پیش بینی فوتبال، یخچال روبی،
  کارخونه روبی، پرچم، اخبار شهر، ایموجی روبی.
- پشتیبانی (ADMIN_IDS) هیچ‌وقت خودش با این محدودیت‌ها مسدود نمی‌شه؛ همیشه دسترسی کامل داره.
- برای «روباه زخمی» غیرفعال کردن یعنی جاب اسپاون تصادفی روباه زخمی توی اون گپ
  دیگه اجرا نمی‌شه.
- پیاده‌سازی: ستون جدید `disabled_features` روی جدول `group_chats` (مهاجرت خودکار
  موقع استارت)، به‌علاوه‌ی هوک‌های `feature_toggle_command` / `feature_blocked` /
  `is_feature_disabled` توی `bot.py`.

## ۳. باگ دکمه‌ی بازگشت کازینو/بازی روبی رفع شد
- علت باگ: هم کازینو و هم بازی روبی از یک متغیر مشترک (`context.user_data['ruby_setup']`)
  برای یادآوری «آخرین بازی انتخاب‌شده» استفاده می‌کردن. اگه کاربر دو تا پنل ساخت میز
  (یکی از کازینو، یکی از بازی روبی) همزمان باز می‌کرد، دکمه‌ی «بازگشت» روی پنل قدیمی‌تر
  بر اساس آخرین انتخاب (نه پنل خودش) عمل می‌کرد؛ به همین خاطر گاهی به‌جای منوی کازینو،
  منوی بازی روبی نشون داده می‌شد.
- راه‌حل: کلید بازی مستقیماً داخل callback_data دکمه‌ی بازگشت ذخیره می‌شه
  (`rubysetup:back:<key>:<owner_id>`) به‌جای تکیه به حافظه‌ی مشترک کاربر؛ پس هر پنل
  همیشه به منوی درست خودش برمی‌گرده، حتی اگه پنل‌های دیگه‌ای هم باز باشن.
