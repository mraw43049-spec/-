# پنل‌های تغییر اسم/جنسیت روباه و ارسال تیکت در همان پیام باز می‌شوند

## روباه
- «✏️ تغییر نام»: به‌جای پیام جدید، همان پنل روباه ویرایش می‌شود و می‌گوید اسم را بفرست
  (با دکمه‌ی 🔙 بازگشت). بعد از فرستادن اسم، سؤال تأیید هم در همان پیام می‌آید؛
  با «بله/خیر» پنل روباه با اسم جدید (یا بدون تغییر) دوباره در همان پیام نشان داده می‌شود.
- «⚧ تغییر جنسیت»: انتخاب، تأیید و نتیجه همه در همان پنل هستند و آخرش به پنل روباه
  برمی‌گردد؛ دکمه‌ی 🔙 بازگشت هم اضافه شد.
- اسم فقط در همان چتی که پنل باز شده قبول می‌شود (پیام‌های چت‌های دیگر اسم حساب نمی‌شوند).

## پشتیبانی
- «📩 ارسال تیکت» همان پنل پشتیبانی را ویرایش می‌کند (با 🔙 بازگشت) و تأیید ارسال تیکت
  هم در همان پیام نوشته می‌شود؛ پنل با دو دکمه برمی‌گردد.
- قبلاً با خودِ باز شدن پنل «پشتیبانی»، پیام بعدی هر چیزی که بود تیکت حساب می‌شد؛
  حالا فقط بعد از زدن «ارسال تیکت» منتظر متن می‌شود.
- دکمه‌های پنل فقط برای صاحب همان پنل کار می‌کنند.
- دکمه‌های قدیمی (`support:open`) هم هنوز کار می‌کنند.

## دیپلوی
فقط `bot.py` را جایگزین کن؛ تغییر دیتابیس لازم نیست.
