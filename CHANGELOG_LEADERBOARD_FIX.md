# رفع باگ لیدربرد جهانی (و پنل بازی‌ها)

## علت
در python-telegram-bot نسخه‌ی ۲۰ به بعد، `InlineKeyboardMarkup.inline_keyboard` یک tuple تغییرناپذیر است.
کد قبلی بعد از ساختن کیبورد، `.insert(...)` / `.extend(...)` صدا می‌زد → `AttributeError`.
چون `q.answer()` قبلش زده شده بود، دکمه فقط «لود» می‌شد و هیچ اتفاقی نمی‌افتاد.
- لیدربرد جهانی (همه‌ی ۵ بخش: رفرال، روب‌پوینت، روباه‌های زخمی، شکار، روب روب) → هر بخش کرش می‌کرد.
- پنل بازی‌های XO، سنگ‌کاغذ‌قیچی، خرگوش‌خور و دوتایی‌ها هم همین الگو را داشتند.

## تغییرات
- کیبورد دیگر تغییر داده نمی‌شود؛ دکمه‌های «👤 پروفایل» تکراریِ زیر لیدربرد و پنل بازی‌ها حذف شدند چون اسم‌ها الان داخل متن آبی و کلیک‌پذیرند (و دکمه‌های tg://user?id بعضی کاربران به‌خاطر حریم خصوصی کل پیام را رد می‌کردند).
- لیدربرد: خطاهای ویرایش دیگر بی‌صدا بلعیده نمی‌شوند (تو لاگ ثبت می‌شوند).
- لیست دونیت‌کننده‌های شهر: اگر دکمه‌های پروفایل رد شدند، لیست بدون آن‌ها نمایش داده می‌شود.
