# فروشگاه روبی: دو بخش اصلی + پیام‌های سفارش روب پوینت

## ۱. منوی اصلی «شاپ روبی / فروشگاه روبی» حالا فقط ۲ دکمه داره
با فرستادن «شاپ روبی» یا «فروشگاه روبی»، کاربر این دو گزینه رو می‌بینه:

- 🦊 خرید روب پوینت
- 🎁 خرید گیفت استارزی

قبلاً منوی اصلی مستقیم ۳ تعرفه گیفت (۱۵/۲۵/۵۰ استارزی) + دکمه روب پوینت رو با هم نشون می‌داد.

## ۲. هر سه گزینه خرید گیفت، یک‌جا تو یک بخش
با زدن «🎁 خرید گیفت استارزی»، صفحه‌ی جدیدی باز می‌شه که هر سه تعرفه‌ی گیفت
(۱۵ / ۲۵ / ۵۰ استارزی) با هم توی همون یک صفحه لیست شدن (تابع‌های جدید `gift_tiers_text` /
`gift_tiers_keyboard`). دکمه‌ی «🔙 بازگشت به فروشگاه» از این صفحه به منوی اصلی (۲ دکمه‌ای) برمی‌گرده.

مسیر ناوبری بعد از این تغییر:
فروشگاه روبی (۲ بخش) → [خرید گیفت استارزی] → انتخاب تعرفه → انتخاب طرح → تعداد → آیدی گیرنده → متن گیفت → رسید
فروشگاه روبی (۲ بخش) → [خرید روب پوینت] → انتخاب بسته → آیدی گیرنده → رسید

callback pattern مربوط به `gift_button` هم برای دو اکشن جدید (`tiers`, `backtiers`) آپدیت شد.

## ۳. پیام بعد از ارسال رسید خرید روب پوینت
وقتی کاربر عکس رسید خرید روب پوینت رو می‌فرسته، ربات دقیقاً این پیام رو می‌ده:

> ✅ شما روب پوینت سفارش داده‌اید (شماره سفارش: …).
> ⏳ منتظر تایید پشتیبانی باشید؛ به‌زودی … روب پوینت به آیدی … اضافه می‌شه.
> در صورت بروز مشکل با پشتیبانی تماس بگیرید: @escotch

(قبلاً متن «سفارش شما ثبت شد» بود که همون معنی رو می‌رسوند ولی عین جمله‌ی خواسته‌شده نبود.)

## ۴. هشدار رسید فیک، قبل از ارسال رسید
تو خلاصه سفارش روب پوینت (`points_order_summary_text`)، همراه شماره کارت، این هشدار هم اضافه شد:

> 🚫 اگه رسید فیک (جعلی) بفرستی، توسط پشتیبانی به‌طور دائم از ربات بن می‌شی.

توجه: بن‌کردن خودکار نیست (چون تشخیص فیک‌بودن رسید نیاز به بررسی انسانی داره)؛ پشتیبانی از همون پنل
`/admin → 🚫 محرومیت کاربر` که از قبل تو ربات هست، کاربر رو بن می‌کنه.

## چیزی که از قبل درست کار می‌کرد (بدون تغییر)
- بعد از ارسال رسید، عکس رسید + کپشن کامل سفارش (آیدی سفارش‌دهنده/گیرنده، مبلغ، مقدار، زمان) با دکمه‌های
  ✅ تایید / ❌ رد برای همه‌ی `ADMIN_IDS` ارسال می‌شه.
- هم‌زمان، آمار سفارش (با آیدی‌های سانسورشده) به کانال `@foxfrenzy_gift` می‌ره و بعد از تصمیم پشتیبانی
  (تایید/رد) همون پیام کانال ادیت و آپدیت می‌شه.

## نکته‌ی مهم دیپلوی
این تغییرات فقط روی فایل `bot.py` پروژه اعمال شده. برای فعال‌شدن روی سرور (مثلاً Railway) باید خودت این
نسخه رو push/آپلود کنی و سرویس ری‌دیپلوی بشه.
