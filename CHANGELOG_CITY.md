# 🏙 شهر روبی (جدید)

## چطور کار می‌کنه
- هر گپی که ربات توشه، خودش یه «شهر روبی» داره (نیازی به ساخت دستی نیست).
- کاربر با نوشتن `شهر روبی` / `شهر روباهیو` / `/شهر` تو همون گپ، پنل شهر رو می‌بینه.
- 🦁 شهردار = مالک گپ؛ با فراخوانی `get_chat_administrators` پیدا می‌شه (نیاز داره ربات بتونه لیست ادمین‌ها رو ببینه).
- زیر پنل یه دکمه‌ی **🏦 دونیت به خزانه شهر** هست؛ با زدنش ربات مبلغ رو می‌پرسه (فرمت مثل بانک: `5000` / `5k` / `5کا`).

## آمار هر بخش (مخصوص همون گپ، نه کل ربات)
- 🐾 **روب روب ها**: تعداد دفعاتی که تو همون گپ «روب روب» زده شده (+ روب‌روب‌های جایزه‌ی نجات روباه زخمی).
- 🦊 **جمعیت**: تعداد روباه‌های زخمی که تو همون گپ نجات داده شدن.
- ⚔️ **شکار ها**: تعداد شکارهای انجام‌شده تو همون گپ (هم از دکمه‌ی روباه، هم از دستور شکار).
- 🏦 **خزانه**: مجموع دونیت‌های کاربرا.
- رتبه‌ی هر بخش، رتبه‌ی همون گپ در مقایسه با بقیه‌ی گپ‌هاییه که ربات توشونه.

## ارتقای سطح
- سطح شهر با عدد رومی نشون داده می‌شه (I تا XI؛ یعنی حداکثر ۱۰ بار ارتقا).
- هدف پایه برای سطح ۱: ۱۵۰ روب‌روب، ۵ روباه زخمی، ۱۰ شکار، ۱۰۰٬۰۰۰ روب‌پوینت خزانه.
- هر ارتقا: هدف روب‌روب/روباه‌زخمی/شکار ×۱.۵ می‌شه، و دارایی مورد نیاز خزانه ×۴ می‌شه (طبق خواسته).
- همین که هر ۴ هدف با هم برآورده بشن (بعد از هر روب‌روب/شکار/نجات/دونیت چک می‌شه)، شهر خودکار ارتقا پیدا می‌کنه:
  - تو گپ پیام تبریک فرستاده می‌شه.
  - به تمام کسایی که از ارتقای قبلی تا الان دونیت کرده بودن، ۲۰۰ روب‌پوینت داده می‌شه و تو پیوی خبردار می‌شن.

## باف‌های شهر (فعلاً ثابت، مستقل از سطح)
- 🐾 روب روب سریع‌تر: کول‌داون «روب روب» تو گپ‌های دارای شهر، ۱۰ ثانیه کمتره.
- 🦊 افزایش جمعیت شهر: فقط به‌عنوان توضیح در پنل نمایش داده می‌شه (چون اسپاون روباه زخمی از قبل مستقل از گپ خاصیه).

## لیدر برد گروهی
- «لیدر برد» → **👥 لیدر برد گروهی** → ۴ گزینه: ⚔️ شکارها، 🐾 روب روب‌ها، 💰 خزانه، 🦊 جمعیت شهر.
- هرکدوم رو بزنی، ۱۰۰ گپ برتر همون بخش با اسم گپ و مقدار نشون داده می‌شه.

## دیتابیس
- فیلدهای جدید روی جدول موجود `group_chats` اضافه شدن (نه جدول جدید)، با همون سیستم migration خودکار پروژه؛ داده‌ی قبلی گپ‌ها پاک نمی‌شه.

> فرض‌های من (چون تو درخواست دقیق نبودن): ضریب رشد هدف‌های روب‌روب/روباه‌زخمی/شکار رو ۱.۵× گذاشتم، و رتبه‌ها رو به‌صورت عدد ساده (#1، #2...) نشون دادم نه به فرمت «هزار» که تو مثال بود (چون تعداد گپ‌ها معمولاً خیلی کمتر از هزاره).
