# رفع باگ درخواست دوستی (دوست روبی)

## مشکل
کاربر آیدی/یوزرنیم دوستش را می‌فرستاد و هیچ اتفاقی نمی‌افتاد: نه پیام «درخواست ارسال شد»، نه درخواستی برای طرف مقابل.

## علت‌ها
1. `resolve_friend_target` برای کاربرِ موجود در دیتابیس، خودِ مدل `User` را برمی‌گرداند و بعد `get_or_create_user(session, <مدل User>)` صدا زده می‌شد؛ مدل `.id` ندارد (فقط `telegram_id`) → `AttributeError` و هیچ پاسخی.
2. حتی بعد از رفع مورد ۱: `user_display_name(target)` بعد از `session.close()` صدا زده می‌شد (`expire_on_commit` پیش‌فرض) → `DetachedInstanceError` قبل از ارسال پیام.
3. در `friend_decision_button` مقدار `req.sender_id` بعد از بستن session خوانده می‌شد؛ چون داخل `try/except: pass` بود، خطا بی‌صدا بلعیده می‌شد و **به فرستنده هیچ‌وقت خبر قبول/رد نمی‌رسید**.
4. رد درخواست هم به «پر بودن ظرفیت دوستان» گیر می‌کرد.

## تغییرات
- `resolve_friend_target` حالا یک شیء ساده `TgIdentity` (id/username/first_name) برمی‌گرداند؛ ورودی فارسی‌رقم، `@username` و لینک `t.me/...` هم پشتیبانی می‌شود.
- ارسال درخواست: ساخت درخواست ← ارسال پیام (دکمه‌های ✅ قبول / ❌ رد) به **پیوی ربات** طرف مقابل ← فقط در صورت موفقیت به فرستنده «درخواست ارسال شد» گفته می‌شود.
  اگر طرف مقابل ربات را استارت نکرده/بلاک کرده باشد، درخواست پاک می‌شود و به فرستنده دلیلش گفته می‌شود (دیگر «ارسال شد» دروغ نمی‌گوید).
- قبول/رد: نتیجه (قبول یا رد) به **پیوی ربات** درخواست‌دهنده اطلاع داده می‌شود، نه در گروه. اگر ارسال شکست بخورد در لاگ ثبت می‌شود.
- رد درخواست دیگر به ظرفیت ربط ندارد؛ قبول‌کردنِ دوباره‌ی دوستیِ موجود، رکورد تکراری نمی‌سازد.
- پنل «دوست روبی» حالا درخواست‌های منتظرِ تأیید را با دکمه‌ی قبول/رد نشان می‌دهد (اگر پیام اولیه گم شد).
- هر خطای پیش‌بینی‌نشده در این فلو لاگ می‌شود و پیام «مشکلی پیش آمد» به کاربر نمایش داده می‌شود (دیگر سکوت نیست).
- اگر کاربر در حالت «افزودن دوست» متنِ دستور فارسی (مثل «روبام») بفرستد، حالت بسته می‌شود و دستور اجرا می‌شود.
- وقتی کاربر پیدا نشد یا خودش را وارد کرد، می‌تواند بدون زدن دوباره‌ی دکمه، آیدی درست را بفرستد.

فقط `bot.py` تغییر کرده است؛ دیتابیس/مایگریشت تغییری ندارد.
