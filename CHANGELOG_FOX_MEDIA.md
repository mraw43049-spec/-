# یاد دادن مدیا به روباه (آهنگ، ویدیو، گیف، استیکر)
- `database.py`: ستون‌های `media_type`، `file_id`، `file_unique_id` به جدول `fox_knowledge` اضافه شد (migration خودکار؛ دیتای قبلی سالم می‌مونه).
- `fox_brain.py`: `set_custom` آیتم مدیا هم قبول می‌کنه؛ `media_lookup` (و حالت `exact`) اضافه شد؛ `custom_lookup` همچنان فقط جواب‌های متنی رو برمی‌گردونه.
- `bot.py`:
  - دستور ادمین (پیوی): `یاد بگیر آهنگ|ویدیو|گیف|استیکر|ویس|عکس|فایل: کلید۱، کلید۲ | کپشن اختیاری` به‌صورت کپشنِ فایل یا ریپلای روی فایل.
  - هندلر جدید `fox_teach_media_caption` (گروه ۴، فقط پیوی ادمین، فقط وقتی کپشن با «یاد بگیر» شروع بشه) — با بقیه‌ی هندلرها تداخل نداره.
  - `ai_chat_run`: قبل از API و مغز متنی، اگه کلید مدیا پیدا بشه فایل رو ریپلای می‌کنه (`send_fox_media`)؛ اگه ارسال خطا داد به جواب متنی برمی‌گرده.
  - `لیست یادگیری` نوع مدیا رو نشون می‌ده؛ `فراموش کن <شماره>` برای مدیا هم کار می‌کنه.
  - `FOX_MEDIA_DIRECT=1` (اختیاری): تو گروه‌ها کل پیام = یه کلید مدیا → بدون صدا زدن روباه جواب می‌ده.
- راهنمای کامل: `AI_SETUP.md` بخش «یاد دادن آهنگ، ویدیو، گیف و استیکر».
