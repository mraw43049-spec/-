# رفع باگ ازدواج روبی — 2026-09-24

- **دکمه‌های خواستگاری (کادو/دسته گل/حلقه/شکلات)**: پنل ازدواج حالا عکس دارد و ویرایش پیام عکس‌دار با `edit_text` خطا می‌داد؛ برای همین با اینکه شکلک در انبار بود، زدن روی دکمه هیچ اتفاقی نمی‌افتاد. همه‌ی ویرایش‌های `marriage_callback` (خواستگاری، قبول/رد، نینی، غذا، ارتقا، برداشت، سقط، طلاق) از `marriage_edit_panel` می‌گذرند.
- **ریپلای حال / بوسه / بغل**: اگر مرحله‌ی نیمه‌کاره‌ای (خواستگاری، انتقال روب‌پوینت، تغییر اسم نینی) در حافظه‌ی کاربر مانده بود، در گروه کل دستور نادیده گرفته می‌شد. حالا ریپلای گروهی مستقل از آن مرحله‌ها کار می‌کند و با باز کردن پنل ازدواج آن مرحله‌های قدیمی پاک می‌شوند.
- **انتخاب خانم در گروه**: اگر پنل در گروه باز شده باشد، آیدی عددی یا @شناسه همان‌جا قبول می‌شود (فقط همین دو قالب، تا پیام‌های عادی گروه اشتباه برداشت نشوند).
- **هدیه‌ی خواستگاری**: بعد از تأیید ارسال خواستگاری، هدیه تا جواب خانم از انبار خواستگار نگه داشته می‌شود.
  - رد شدن، جواب ندادن تا ۱۲ ساعت، یا نرسیدن پیام به خانم → هدیه به انبار شکلک برمی‌گردد (بدون دوبل شدن) و خواستگار پیام می‌گیرد.
  - قبول شدن → هدیه تحویل می‌شود (همان رفتار قبلی).
  - یک job هر ۶۰ ثانیه درخواست‌های منقضی را می‌بندد و هدیه را برمی‌گرداند.
  - درخواست‌های قدیمیِ در انتظار (که هدیه‌شان هنوز در انبار است) بدون مشکل کار می‌کنند.
- تغییری در دیتابیس لازم نیست.

## به‌روزرسانی بعدی — همون روز

- **تکرار پیام «نینی روباه شما متولد شد»**: بعد از رسیدن رابطه به ۵۰/۵۰، هر بار ریپلای حال/بوسه/بغل دوباره پیام تولد نینی رو می‌فرستاد (چون شرط فقط چک می‌کرد رابطه==۵۰ باشه، نه اینکه همین الان تولد اتفاق افتاده باشه). الان پیام بارداری و پیام تولد فقط دقیقاً همون لحظه‌ای که واقعاً اتفاق می‌افتن نمایش داده می‌شن.
- **غذا دادن به نینی**: اگه هیچ شکار یا تخم‌مرغی نداشته باشی، به‌جای یه لیست خالی، الان پیام واضح میده که برو شکار کن یا از مارکت روبی تخم‌مرغ بخر.
- **تولید روب‌پوینت نینی**: منطق از قبل طبق همون قانون بود (فقط وقتی هر دو والد سیرن تولید می‌کنه)؛ نینی خودش هم باید سیر باشه، پس تغذیه‌ی منظم لازمه. حالا که دکمه‌ی غذا دادن (با فیکس عکس پنل) درست کار می‌کنه، تولید هم طبق همون قانون شروع می‌شه.
- **دکمه برداشت و تقسیم روب‌پوینت نینی**: از قبل داخل پنل ازدواج بود («💰 برداشت روب‌پوینت نینی»، وقتی نینی چیزی جمع کرده باشه) و ۵۰/۵۰ بین پدر و مادر تقسیم می‌کنه؛ چون قبلاً به خاطر باگ عکسِ پنل و باگ تولید دیده نمی‌شد، الان که هر دو حل شدن این دکمه هم درست ظاهر می‌شه.
- **عکس هنگام تایپ «نینی روبی»**: دیگه عکس ازدواج زوج نشون داده نمی‌شه. اگه فایلی به اسم `baby_panel.png` (عکس انیمیشنی روباه نوزاد) کنار `marriage_panel.png` تو پوشه‌ی ربات بذاری، همون نمایش داده می‌شه؛ اگه نذاری، فعلاً فقط متن (بدون عکس) نشون داده می‌شه — دیگه هیچ‌وقت عکس ازدواج به جاش نمیاد.

## به‌روزرسانی بعدی

- **عکس نینی روبی**: `baby_panel.png` (عکس بچه‌روباه انیمیشنی) اضافه شد؛ حالا وقتی کسی «نینی روبی» بنویسد همین عکس نشون داده می‌شه، نه عکس ازدواج زوج.
- **«روباهیو درس» از گروه**: قبلاً دکمه‌ی «ورود به پنل درس» فقط کاربر رو به پیوی ربات می‌برد و باید دوباره «روباهیو درس» تایپ می‌کرد. الان همون دکمه با لینک `?start=edu_panel` پنل درس رو مستقیم تو پیوی باز می‌کنه.
