# رفع باگ ازدواج روبی — 2026-09-24

- **دکمه‌های خواستگاری (کادو/دسته گل/حلقه/شکلات)**: پنل ازدواج حالا عکس دارد و ویرایش پیام عکس‌دار با `edit_text` خطا می‌داد؛ برای همین با اینکه شکلک در انبار بود، زدن روی دکمه هیچ اتفاقی نمی‌افتاد. همه‌ی ویرایش‌های `marriage_callback` (خواستگاری، قبول/رد، نینی، غذا، ارتقا، برداشت، سقط، طلاق) از `marriage_edit_panel` می‌گذرند.
- **ریپلای حال / بوسه / بغل**: اگر مرحله‌ی نیمه‌کاره‌ای (خواستگاری، انتقال روب‌پوینت، تغییر اسم نینی) در حافظه‌ی کاربر مانده بود، در گروه کل دستور نادیده گرفته می‌شد. حالا ریپلای گروهی مستقل از آن مرحله‌ها کار می‌کند و با باز کردن پنل ازدواج آن مرحله‌های قدیمی پاک می‌شوند.
- **انتخاب خانم در گروه**: اگر پنل در گروه باز شده باشد، آیدی عددی یا @شناسه همان‌جا قبول می‌شود (فقط همین دو قالب، تا پیام‌های عادی گروه اشتباه برداشت نشوند).
- **هدیه‌ی خواستگاری**: بعد از تأیید ارسال خواستگاری، هدیه تا جواب خانم از انبار خواستگار نگه داشته می‌شود.
  - رد شدن، جواب ندادن تا ۱۲ ساعت، یا نرسیدن پیام به خانم → هدیه به انبار شکلک برمی‌گردد (بدون دوبل شدن) و خواستگار پیام می‌گیرد.
  - قبول شدن → هدیه تحویل می‌شود (همان رفتار قبلی).
  - یک job هر ۶۰ ثانیه درخواست‌های منقضی را می‌بندد و هدیه را برمی‌گرداند.
  - درخواست‌های قدیمیِ در انتظار (که هدیه‌شان هنوز در انبار است) بدون مشکل کار می‌کنند.
- تغییری در دیتابیس لازم نیست.
