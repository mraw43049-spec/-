# رفع باگ روب‌پوینت + انبار محصول و بازار نوسانی 🏭📦

## باگ روب‌پوینت (رفع شد)
کارخونه (ساخت، سفارش تولید، ارتقاها) داشت موجودی رو از ستون قدیمی و جدا `points` چک می‌کرد،
در حالی که موجودی واقعی‌ای که همه‌جای ربات (پروفایل، شکار، یخچال و...) بهش «روب‌پوینت» می‌گن
ستون `fox_points` است. برای همین با اینکه روب‌پوینت داشتی، کارخونه همیشه می‌گفت کافی نیست.
همه‌ی چک‌ها و کم‌کردن‌های کارخونه الان از `fox_points` استفاده می‌کنند.

## چیزی که عوض شد: محصول تولیدشده دیگه خودکار فروخته نمی‌شه
- دکمه‌ی «💰 برداشت» حالا محصول رو به **انبار محصول** کاربر اضافه می‌کنه، نه اینکه خودکار بفروشدش.
- منوی جدید «📦 انبار محصول (فروش)» تو پنل کارخونه اضافه شد. اونجا برای هر محصولِ باز‌شده:
  - موجودی کاربر از اون محصول
  - قیمت الان بازار
  - بیشترین قیمتی که تا الان ثبت شده
  - کمترین قیمتی که تا الان ثبت شده
  نشون داده می‌شه، و اگه از یک محصول موجودی داشته باشی دکمه‌ی فروش همون محصول (یا فروش کل انبار)
  ظاهر می‌شه.
- قیمت هر محصول **هر ۲۵ دقیقه** با یک جاب پس‌زمینه (`update_market_prices_job`) به‌طور تصادفی عوض
  می‌شه: بین ۵۵٪ تا ۱۰۰٪ همون عددی که قبلاً «سقف قیمت فروش» هر محصول بود. یعنی قیمت هیچ‌وقت از
  عدد قبلی بیشتر نمی‌شه، فقط بین کف و سقف نوسان می‌کنه.

## فرضیاتی که به‌خاطر ابهام انتخاب شدند (لطفاً بررسی کن)
1. **بازه‌ی نوسان قیمت** = ۵۵٪ تا ۱۰۰٪ سقف قبلی هر محصول. عدد ۵۵٪ فقط یک فرض معقول بود؛ اگه بازه‌ی
   دیگه‌ای مدنظرته (مثلاً ۳۰٪ تا ۱۲۰٪، یا قیمت‌ها گاهی از سقف قبلی هم بیشتر بشن) بگو تا عوضش کنم.
2. **بیشترین/کمترین قیمت نمایش داده‌شده** از لحظه‌ی اجرای این نسخه به بعد ثبت می‌شه (نه صرفاً «امروز»)،
   چون مشخص نبود بازه‌ی زمانیش باید روزانه ریست بشه یا نه. اگه می‌خوای هر روز نیمه‌شب ریست بشه بگو تا
   اضافه‌ش کنم.
3. **قیمت‌ها سراسری‌اند** (یک قیمت برای همه‌ی کاربرها)، نه اینکه هر کاربر قیمت جدا داشته باشه؛ چون بازار
   طبیعتاً مشترکه.
4. سرعت تولید و هزینه‌ی ساخت هر محصول (که قبلاً توضیح داده شده بودن) دست‌نخورده موندن؛ فقط «فروش» از
   کارخونه جدا شد و رفت تو انبار/بازار.

اگه هرکدوم از این فرضیات با چیزی که تو ذهنت بود فرق داره، بگو تا اصلاحش کنم.

## نکته‌ی مهم دیپلوی
این تغییرات فقط روی فایل‌های پروژه (`bot.py`, `database.py`, `game_logic.py`) اعمال شده. برای فعال‌شدن
روی Railway باید خودت این نسخه رو push کنی روی گیت‌هاب (یا مستقیم آپلود کنی) و Railway ری‌دیپلوی بشه —
من به اینترنت/گیت‌هاب/Railway دسترسی مستقیم ندارم. دیتابیس نیازی به migration دستی نداره؛ `init_db()`
همون اول اجرا جدول‌های جدید (`factory_inventory`, `market_prices`) رو خودش می‌سازه.
