# افزودن بخش «خرید روب پوینت 🦊» به فروشگاه روبی

## چه چیزی اضافه شد
- یک دکمه جدید «🦊 خرید روب پوینت» داخل «فروشگاه روبی» (همون منویی که با /شاپ روبی یا دکمه «شاپ روبی» باز میشه).
- ۴ بسته خرید:
  1. 250,000 روب پوینت — 15,000 تومان
  2. 500,000 روب پوینت — 22,000 تومان
  3. 1,000,000 روب پوینت — 45,000 تومان
  4. 3,000,000 روب پوینت — 130,000 تومان

## جریان سفارش کاربر
1. کاربر بسته مورد نظر رو انتخاب می‌کنه.
2. آیدی عددی کاربری که باید روب پوینت رو دریافت کنه رو می‌فرسته (اعتبارسنجی با regex عدد ۵ تا ۱۵ رقمی).
3. خلاصه سفارش + شماره کارت (همون کارت فروشگاه گیفت: 6219861851160068 به نام ظریفی) نمایش داده می‌شه.
4. کاربر عکس رسید پرداخت رو می‌فرسته؛ اگه چیز دیگه‌ای بفرسته فقط ازش خواسته می‌شه دوباره فقط عکس رسید بفرسته (بن نمی‌شه).

## اطلاع‌رسانی
- **به پشتیبانی (ادمین‌ها):** عکس رسید + کپشن کامل شامل آیدی عددیِ *بدون سانسور* سفارش‌دهنده و گیرنده، مبلغ، مقدار روب پوینت، و زمان دقیق شمسی همراه با دو دکمه «✅ تایید» و «❌ رد».
- **به کانال @foxfrenzy_gift:** همون سفارش با آیدی‌های سانسورشده (mask_telegram_id) و بدون دکمه تصمیم‌گیری، صرفاً برای نمایش عمومی وضعیت سفارش (در حال بررسی / تایید شد / رد شد؛ پیام کانال بعد از تصمیم پشتیبانی ادیت می‌شه).

## تصمیم پشتیبانی
- با زدن «✅ تایید»: مقدار روب پوینتِ بسته به `fox_points` کاربر گیرنده (بر اساس آیدی عددی وارد شده) اضافه می‌شه؛ اگه اون کاربر قبلاً هیچ‌وقت ربات رو استارت نکرده باشه، ردیفش در دیتابیس ساخته می‌شه تا موجودیش حفظ بمونه. پیام تایید به سفارش‌دهنده و گیرنده ارسال و پیام کانال آپدیت می‌شه.
- با زدن «❌ رد»: سفارش رد می‌شه، به سفارش‌دهنده اطلاع داده می‌شه (همراه آیدی پشتیبانی @escotch برای پیگیری) و پیام کانال آپدیت می‌شه.
- هر سفارش فقط یک‌بار قابل تصمیم‌گیریه (وضعیت pending → approved/rejected).

## تغییرات فنی
- مدل جدید `PointsPurchase` در `database.py`.
- توابع و هندلرهای جدید در `bot.py`: `points_shop_text/keyboard`, `points_button`, `handle_points_text`, `finalize_points_order`, `points_flow_gate`, `points_admin_button` و ثبت callback patternهای `^points:...` و `^pts:(approve|reject):\d+$`.
- دکمه ورودی جدید داخل `gift_shop_keyboard()`.
