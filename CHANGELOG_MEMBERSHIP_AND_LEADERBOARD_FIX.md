# تغییرات این نسخه

## ۱. عضویت اجباری در دو کانال
حالا برای استفاده از ربات، کاربر باید هم عضو کانال اصلی باشه هم عضو `@foxfrenzy_gift`.

- کانال دوم از طریق متغیرهای محیطی جدید در Railway قابل تنظیمه:
  - `REQUIRED_CHANNEL_2` (پیش‌فرض: `@foxfrenzy_gift`)
  - `REQUIRED_CHANNEL_2_URL` (پیش‌فرض: `https://t.me/foxfrenzy_gift`)
- پیام «عضویت اجباری» و دکمه «عضو شدم، بررسی کن» حالا هر دو کانال رو با دو دکمه جداگانه نشون می‌ده.
- بررسی عضویت (چه موقع ورود به ربات، چه موقع زدن دکمه «بررسی کن») روی **هر دو** کانال انجام می‌شه؛ اگر
  کاربر فقط عضو یکی از دو کانال باشه، هنوز دسترسی نداره.
- ادمین‌ها (`ADMIN_IDS`) طبق قبل از این محدودیت مستثنا هستن.

## ۲. رفع باگ لیدر برد بخش «شکار»
باگ پیدا شد: وقتی کاربر شکار رو با **دستور متنی/اسلش** (`/شکار` یا ریپلای روی پیام) شروع می‌کرد، فیلد
`hunt_count` کاربر آپدیت نمی‌شد (فقط تو مسیر دکمه‌ی شیشه‌ای این فیلد افزایش پیدا می‌کرد). در نتیجه
شکارهایی که از این مسیر انجام می‌شدن اصلاً تو لیدر برد «⚔️ شکار» حساب نمی‌شدن.

رفع شد: حالا در هر دو مسیر (دکمه‌ی شیشه‌ای و دستور متنی) `hunt_count` کاربر به‌درستی افزایش پیدا می‌کنه و
تو لیدر برد جهانی/رتبه‌بندی شهر همه‌ی شکارها لحاظ می‌شن. این تغییر فقط از این به بعد رو اصلاح می‌کنه؛
شکارهایی که قبل از این آپدیت از مسیر متنی انجام شده بودن، در دیتابیس ثبت نشده بودن و قابل بازیابی نیستن.

## ۳. فاصله‌گذاری و صفحه‌بندی لیدر برد (جهانی + گروهی)
قبلاً لیدر برد ۱۰۰ نفره در یک پیام واحد نمایش داده می‌شد و اگه متن از ۴۰۰۰ کاراکتر بیشتر می‌شد، بقیه‌ی
نفرات به‌سادگی **قطع/حذف** می‌شدن (`text[:4000] + "…"`).

حالا:
- هر ۱۰ نفر یک صفحه‌ست (`LEADERBOARD_PAGE_SIZE = 10`) و بین هر نفر با نفر بعدی یک خط فاصله گذاشته می‌شه.
- اگه تعداد نفرات (تا سقف ۱۰۰ نفر، `LEADERBOARD_LIMIT`) بیشتر از یک صفحه باشه، دکمه‌های «⬅️ قبلی» و
  «بعدی ➡️» زیر پیام اضافه می‌شن تا بشه بین صفحات رفت و برگشت کرد — بدون اینکه هیچ نفری از رتبه‌بندی
  حذف بشه.
- این تغییر هم برای «🌍 لیدر برد جهانی» (۴ دسته) و هم برای «👥 لیدر برد گروهی» (۴ دسته شهر) به‌صورت
  یکسان اعمال شده.
- شماره صفحه پایین هر پیام نشون داده می‌شه (مثلاً «📄 صفحه ۲ از ۵»).

هیچ داده‌ای از کاربرها یا گروه‌ها حذف/تغییر نکرده؛ فقط نحوه‌ی نمایش لیدر برد اصلاح شده.
