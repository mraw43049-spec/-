# 🦊 روباه هوشمند — راهنما

## بدون هیچ تنظیمی کار می‌کنه
«مغز قانون‌محور» (`fox_brain.py`) رایگانه، روی خود سرور ربات اجرا می‌شه و به هیچ سرویس بیرونی وصل نمی‌شه. فقط دیپلوی کن.

| قابلیت | چطور استفاده کنیم |
|---|---|
| 🦊 **گفتگو با روباه** | پیوی ربات: هر چی بنویسی. گروه: `روباهیو سلام…` / `روباه جون …` / منشن ربات / ریپلای روی جواب‌های روباه. سلام، احوال‌پرسی، جوک، فکت، دلداری، «من کی‌ام؟» و … |
| 📖 **راهنمای هوشمند** | `راهنما` (لیست موضوع‌ها) یا `راهنما بانک`، `راهنما کازینو`، … یا تو گفتگو یه سوال بپرس («بانک چیه؟»). جواب از راهنمای واقعی ربات پیدا می‌شه |
| 🛡 **مدیریت هوشمند گروه** | ادمین گروه: `مدیریت هوشمند روشن` (پیش‌فرض خاموشه) / `مدیریت هوشمند خاموش` / `مدیریت هوشمند` (وضعیت) |
| 📰 **اخبار شهر** | تو گروه: `اخبار شهر` — خبر از روی عددهای واقعی شهر. با ارتقای شهر هم خودکار یه خبر شاد میاد |

### مدیریت هوشمند دقیقاً چی رو حذف می‌کنه؟
- فحش و توهین واضح (فارسی و چند فینگلیش/انگلیسی رایج؛ حتی «ک.ی.ر» و «ک ی ر»)
- تبلیغ: لینک دعوت تلگرام، لینک/منشن کانال همراه با کلمه‌های تبلیغی («عضو شو»، «تخفیف»، …)، شماره‌ی تماس + فروش
- اسپم: یه پیام تکراری ۴ بار تو ۶۰ ثانیه، یا ۵ منشن یک‌جا
- اخطار ۱ و ۲ = حذف پیام + هشدار. **سومین تخلف در ۲۴ ساعت = ۱۰ دقیقه سکوت.**
- ادمین‌های گروه و ادمین‌های ربات بررسی نمی‌شن.
- ربات باید ادمین گروه با دسترسی «حذف پیام‌ها» (و برای سکوت «محدود کردن اعضا») باشه.

### تنظیم لیست کلمه‌ها (اختیاری، از Railway → Variables)
- `MOD_EXTRA_WORDS` = `کلمه۱,کلمه۲` → کلمه‌های بیشتر برای حذف
- `MOD_WHITELIST_WORDS` = `عوضی,...` → کلمه‌هایی که نباید حذف بشن (مثلاً گروه‌های صمیمی که با هم شوخی می‌کنن)
- `FOX_BRAIN` = `0` → خاموش کردن کامل مغز قانون‌محور

## ✍️ یاد دادن دستی به روباه (بدون دست زدن به کد)
فقط ادمین‌های ربات، **تو پیوی ربات**:

| دستور | کار |
|---|---|
| `یاد بگیر: کلید۱، کلید۲ \| جواب` | یه جواب جدید اضافه می‌کنه. هر وقت کسی یکی از کلیدها رو تو پیامش بنویسه، روباه همون جواب رو می‌ده |
| `لیست یادگیری` | آخرین ۴۰ مورد رو با شماره نشون می‌ده |
| `فراموش کن <شماره>` | یه مورد رو حذف می‌کنه |

**مثال‌ها:**
- `یاد بگیر: کانال، آدرس کانال | کانال رسمی ما: @foxio`
- `یاد بگیر: قیمت شاپ | همه‌ی قیمت‌ها تو «شاپ روبی» هست 🎁`
- چند جواب برای یه کلید (تصادفی انتخاب می‌شه): چند بار `یاد بگیر: جوک | جوک اول…` و `یاد بگیر: جوک | جوک دوم…` بنویس.
- تو جواب می‌تونی از `{name}` (اسم کاربر)، `{level}` (لول) و `{fox}` (اسم روباهش) استفاده کنی. جواب می‌تونه چند خطی باشه.

نکته‌ها: کلیدها بین ۲ تا ۴۰ حرفن، جواب تا ۱۵۰۰ حرف، و حداکثر ۳۰۰ مورد. جواب‌های دستی روی جواب‌های آماده اولویت دارن (جز پیام‌های نگران‌کننده). برای ویرایش، مورد قبلی رو با «فراموش کن» پاک کن و دوباره بنویس. برای اینکه ببینی درست کار می‌کنه، کلیدش رو تو پیوی ربات بنویس.

## 🎵🎬🎞😎 یاد دادن آهنگ، ویدیو، گیف و استیکر
روباه می‌تونه به‌جای متن، فایل بفرسته. فقط ادمین‌های ربات، **تو پیوی ربات** (دقیقاً مثل «یاد بگیر» متنی):

| کار | چطور |
|---|---|
| 🎵 آهنگ | فایل mp3 رو بفرست و تو کپشنش بنویس: `یاد بگیر آهنگ: کلید۱، کلید۲` |
| 🎬 ویدیو | ویدیو رو با کپشن `یاد بگیر ویدیو: کلید` بفرست |
| 🎞 گیف | گیف رو با کپشن `یاد بگیر گیف: کلید` بفرست |
| 😎 استیکر | استیکر کپشن نداره؛ **روی استیکر ریپلای کن** و بنویس `یاد بگیر استیکر: کلید` |
| ریپلای | برای هر نوعی می‌تونی روی همون فایل (حتی فوروارد شده از کانال/گروه) ریپلای کنی: `یاد بگیر آهنگ: کلید۱، کلید۲` |
| ویس / عکس / فایل | همین روش: `یاد بگیر ویس: …` / `یاد بگیر عکس: …` / `یاد بگیر فایل: …` |

- نوشتن نوع (آهنگ/ویدیو/گیف/استیکر) اختیاریه: `یاد بگیر: کلید` هم نوع فایل رو خودش تشخیص می‌ده. اگه نوع رو اشتباه بنویسی (مثلاً گیف رو «ویدیو» بنویسی) خطا می‌ده.
- کپشن اختیاری بعد از `|`: `یاد بگیر گیف: خنده، هه | اینم از خنده {name} 😂` (تو استیکر، کپشن جدا بعد از استیکر میاد).
- چند فایل برای یه کلید = هر بار یکیشون تصادفی (مثلاً چند آهنگ برای «آهنگ»).
- کاربر تو پیوی ربات کلید رو بنویسه، یا تو گروه «روباهیو آهنگ» / منشن / ریپلای روی جواب روباه، فایل رو می‌گیره.
- `لیست یادگیری` مدیاها رو با آیکون نوعشون نشون می‌ده؛ حذف: `فراموش کن <شماره>` (مثل قبل).
- اختیاری: متغیر `FOX_MEDIA_DIRECT` = `1` تو Railway → تو گروه‌ها وقتی **کل پیام دقیقاً یه کلید** باشه (مثلاً فقط «آهنگ») بدون صدا زدن روباه هم فایل می‌فرسته. پیش‌فرض خاموشه.
- فایل خودش تو دیتابیس ذخیره نمی‌شه؛ فقط `file_id` تلگرام (کوچیک) ذخیره می‌شه، پس فایل باید تو تلگرام بمونه. `file_id` مخصوص همون توکن رباته؛ اگه توکن ربات عوض بشه باید دوباره یاد بدی.
- ظرفیت کل (متن + مدیا) همون ۳۰۰ مورده. پیام‌های نگران‌کننده هیچ‌وقت مدیا نمی‌گیرن.

## 🎶 «روباهیو حال» — آهنگ متناسب با حال کاربر
کاربر می‌نویسه `روباهیو حال` (تو پیوی فقط `حال` هم کافیه؛ تو گروه حتماً با اسم روباه). روباه **۵ سوال ۳گزینه‌ای** (دکمه‌ای) می‌پرسه، از روی جواب‌ها حال کاربر (شاد، آروم، غمگین، پرانرژی، عاشقانه، عصبی) رو حساب می‌کنه و یکی از آهنگ‌هایی که پشتیبانی برای همون حال اضافه کرده رو می‌فرسته، با این متن: «امیدوارم این حالتو خوب کنه🦊🥰».

- فقط خود کاربر می‌تونه دکمه‌های سوال‌های خودش رو بزنه.
- سوال‌ها هر روز (به وقت ایران) عوض می‌شن: ۳۰ سوال تو چرخه‌ی ۶ روزه پخش شدن؛ تو یه روز تکراری نیست و دو روز پشت‌سرهم هم سوال مشترک ندارن. برای سوال‌های بیشتر، لیست `FOX_MOOD_QUESTIONS` تو `bot.py` رو زیاد کن (هر سوال = متن + ۳ گزینه با حالِ هر گزینه).
- اگه برای یه حال آهنگی نباشه، از نزدیک‌ترین حال انتخاب می‌شه؛ اگه هیچ آهنگی تو ربات نباشه، ربات همون اول می‌گه هنوز آهنگی نیست.
- پشت‌سرهم یه آهنگ تکراری به یه نفر نمیاد (اگه چند آهنگ برای اون حال باشه).

**اضافه کردن آهنگ (فقط ادمین، تو پیوی ربات):**
| کار | دستور |
|---|---|
| با کپشن | آهنگ (Music یا mp3) رو بفرست و تو کپشن بنویس: `آهنگ حال: شاد` |
| ریپلای | روی آهنگ ریپلای کن: `آهنگ حال: آروم، عاشقانه` (چند حال با ویرگول) |
| لیست | `لیست آهنگ حال` |
| حذف | `حذف آهنگ حال <شماره>` |

حال‌های قابل قبول: شاد، آروم، غمگین، پرانرژی، عاشقانه، عصبی. اگه همون آهنگ رو دوباره با حال دیگه بفرستی، حال جدید بهش اضافه می‌شه (آهنگ تکراری نمی‌سازه).
این آهنگ‌ها جدا از کلیدهای «یاد بگیر آهنگ: ...» هستن و فقط تو «روباهیو حال» میان.

## 📻 کانال آهنگ (اضافه کردن دسته‌جمعی آهنگ برای «روباهیو حال»)
1. یه کانال بساز (خصوصی هم می‌شه) و **ربات رو ادمین کانال کن** (فقط وقتی تو، به‌عنوان ادمین ربات، این کار رو بکنی کانال ثبت می‌شه؛ کانال هر آدم دیگه‌ای نادیده گرفته می‌شه). ربات تو پیوی بهت پیام تأیید می‌ده.
2. هر آهنگ (Music یا mp3) رو تو کانال بذار و تو کپشنش هشتگ حال بنویس: `#شاد` `#آروم` `#غمگین` `#پرانرژی` `#عاشقانه` `#عصبی` (چند هشتگ = چند حال).
3. ربات خودش آهنگ رو اضافه می‌کنه و با 👍 نشون می‌ده؛ اگه هشتگ حال نداشته باشه 🤔. اگه بعداً کپشن رو ویرایش کنی، حال‌های آهنگ هم اصلاح می‌شن.
- اگه ربات از قبل تو کانال ادمین بوده: تو پیوی بنویس `کانال آهنگ <آیدی عددی کانال>` (مثل `-1001234567890`)؛ `کانال آهنگ` تنها، لیست کانال‌های ثبت‌شده رو نشون می‌ده.
- روش دیگه: متغیر `MOOD_CHANNEL_IDS` تو Railway (آیدی کانال‌ها با ویرگول).
- ری‌اکشن‌های 👍/🤔 فقط وقتی دیده می‌شن که ری‌اکشن تو کانال فعال باشه؛ حتی بدون اون، آهنگ اضافه می‌شه (با «لیست آهنگ حال» چک کن).

## 🦊 «آیا منظورت ... بود؟» و غلط‌گیر املایی
- **غلط تایپی دستورها:** اگه کل پیام شبیه یه دستور ربات باشه ولی اشتباه نوشته شده باشه، ربات می‌گه «آیا منظورت «کازینو» بود؟». (گازینو، کازنو، کازینوو، شاب روبی، لیدربورد، انتقال روپ پوینت 50 و ...). تو جمله‌ها و پیام‌های معمولی هیچ‌وقت دخالت نمی‌کنه. دستورهای کوتاه‌تر از ۵ حرف فقط وقتی پیشنهاد می‌شن که همه‌ی حرف‌ها هم‌صدا باشن (مثل «بانگ» → «بانک»).
- **غلط‌گیر املایی:** غلط‌های رایج و مطمئن (قذا ← غذا، ازیت ← اذیت، حوسله ← حوصله، خدافظ ← خداحافظ، بلاخره ← بالاخره، مشگل ← مشکل، واقعن ← واقعاً، بخاطر ← به خاطر، مطمعن ← مطمئن ...) رو با یه ریپلای کوتاه تصحیح می‌کنه. حداکثر ۳ غلط تو هر پیام، هر نفر حداکثر ۴۵ ثانیه‌ای یه بار (تو پیوی ۲۰ ثانیه) و تو هر گروه حداکثر ۱۲ ثانیه‌ای یه بار.
- روشن/خاموش: تو گروه (فقط ادمین‌های گروه) `غلط گیر روشن` / `غلط گیر خاموش`؛ تو پیوی هر کاربر برای خودش. پیش‌فرض: روشن (`FOX_SPELL_DEFAULT=0` تو Railway اگه می‌خوای پیش‌فرض خاموش باشه؛ ادمین‌های گروه بازم می‌تونن روشنش کنن).
- این یه غلط‌گیر «فهرستی» و آفلاینه (بدون API)، نه یه فرهنگ لغت کامل فارسی؛ فقط غلط‌هایی که تو فهرستش هست رو می‌گیره تا اشتباهاً کلمه‌ی درست رو غلط حساب نکنه. برای اضافه کردن غلط‌های جدید `_EXPLICIT` تو فایل `fox_spell.py` رو ببین (`'درست': 'غلط۱ غلط۲'`).

### اضافه کردن مستقیم تو کد (برای کارهای بزرگ‌تر)
- جوک‌ها و فکت‌ها: لیست `_JOKES` و `_FACTS` بالای `fox_brain.py`
- موضوع‌های گفتگو (سلام، تشکر، …): لیست `_INTENTS` تو `fox_brain.py` (عبارت‌ها + جواب‌ها)
- موضوع‌های راهنما: لیست `extra` داخل تابع `build_guide_entries` تو `bot.py`
- کلمه‌های ممنوع ناظر: متغیر `MOD_EXTRA_WORDS` تو Railway (ساده‌ترین راه)

## محدودیت‌های صادقانه
این «هوش مصنوعیِ واقعی» نیست؛ فقط چیزهایی رو می‌فهمه که براشون قانون نوشته شده. جمله‌های پیچیده یا موضوع‌های بیرون از ربات رو نمی‌فهمه و جواب پیش‌فرض می‌ده. ناظر هم فقط کلمه‌ها و الگوهای شناخته‌شده رو می‌گیره و ممکنه فحش‌های خلاقانه رو رد کنه یا (به‌ندرت) اشتباه بگیره؛ برای همین پیش‌فرض خاموشه و لیست قابل تنظیمه.

## اتصال هوش مصنوعی واقعی (کاملاً اختیاری)
اگه روزی به یه API دسترسی داشتی، فقط `AI_API_KEY` (و برای سرویس‌های سازگار با OpenAI: `AI_PROVIDER=openai`، `AI_BASE_URL`، `AI_MODEL`) رو تو Variables بذار. اون‌وقت گفتگو، راهنما و اخبار شهر اول از API جواب می‌گیرن و اگه API خطا داد یا سقفش پر شد **خودکار به مغز قانون‌محور برمی‌گردن**. ناظر گروه همیشه محلی می‌مونه.
متغیرهای سقف مصرف: `AI_USER_COOLDOWN_SECONDS` (۶)، `AI_USER_DAILY_LIMIT` (۲۵)، `AI_CHAT_DAILY_BUDGET` (۳۰۰۰)، `AI_NEWS_DAILY_BUDGET` (۳۰۰).

## ابزار ادمین (پیوی ربات، فقط ADMIN_IDS)
- `وضعیت هوش مصنوعی` → وضعیت مغز قانون‌محور و API
- `تست هوش مصنوعی` → یه تست سریع از گفتگو، راهنما و ناظر (یا تست اتصال API اگه وصل باشه)
