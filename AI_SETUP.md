# 🦊 روباه هوشمند — راهنما

## بدون هیچ تنظیمی کار می‌کنه
«مغز قانون‌محور» (`fox_brain.py`) رایگانه، روی خود سرور ربات اجرا می‌شه و به هیچ سرویس بیرونی وصل نمی‌شه. فقط دیپلوی کن.

| قابلیت | چطور استفاده کنیم |
|---|---|
| 🦊 **گفتگو با روباه** | پیوی ربات: هر چی بنویسی. گروه: `روباهیو سلام…` / `روباه جون …` / منشن ربات / ریپلای روی جواب‌های روباه. سلام، احوال‌پرسی، جوک، فکت، دلداری، «من کی‌ام؟» و … |
| 📖 **راهنمای هوشمند** | `راهنما` (لیست موضوع‌ها) یا `راهنما بانک`، `راهنما کازینو`، … یا تو گفتگو یه سوال بپرس («بانک چیه؟»). جواب از راهنمای واقعی ربات پیدا می‌شه |
| 🛡 **مدیریت هوشمند گروه** | ادمین گروه: `مدیریت هوشمند روشن` (پیش‌فرض خاموشه) / `مدیریت هوشمند خاموش` / `مدیریت هوشمند` (وضعیت) |
| 📰 **اخبار شهر** | تو گروه: `اخبار شهر` — خبر از روی عددهای واقعی شهر. با ارتقای شهر هم خودکار یه خبر شاد میاد |

### مدیریت هوشمند دقیقاً چی رو حذف می‌کنه؟
- فحش و توهین واضح (فارسی و چند فینگلیش/انگلیسی رایج؛ حتی «ک.ی.ر» و «ک ی ر»)
- تبلیغ: لینک دعوت تلگرام، لینک/منشن کانال همراه با کلمه‌های تبلیغی («عضو شو»، «تخفیف»، …)، شماره‌ی تماس + فروش
- اسپم: یه پیام تکراری ۴ بار تو ۶۰ ثانیه، یا ۵ منشن یک‌جا
- اخطار ۱ و ۲ = حذف پیام + هشدار. **سومین تخلف در ۲۴ ساعت = ۱۰ دقیقه سکوت.**
- ادمین‌های گروه و ادمین‌های ربات بررسی نمی‌شن.
- ربات باید ادمین گروه با دسترسی «حذف پیام‌ها» (و برای سکوت «محدود کردن اعضا») باشه.

### تنظیم لیست کلمه‌ها (اختیاری، از Railway → Variables)
- `MOD_EXTRA_WORDS` = `کلمه۱,کلمه۲` → کلمه‌های بیشتر برای حذف
- `MOD_WHITELIST_WORDS` = `عوضی,...` → کلمه‌هایی که نباید حذف بشن (مثلاً گروه‌های صمیمی که با هم شوخی می‌کنن)
- `FOX_BRAIN` = `0` → خاموش کردن کامل مغز قانون‌محور

## محدودیت‌های صادقانه
این «هوش مصنوعیِ واقعی» نیست؛ فقط چیزهایی رو می‌فهمه که براشون قانون نوشته شده. جمله‌های پیچیده یا موضوع‌های بیرون از ربات رو نمی‌فهمه و جواب پیش‌فرض می‌ده. ناظر هم فقط کلمه‌ها و الگوهای شناخته‌شده رو می‌گیره و ممکنه فحش‌های خلاقانه رو رد کنه یا (به‌ندرت) اشتباه بگیره؛ برای همین پیش‌فرض خاموشه و لیست قابل تنظیمه.

## اتصال هوش مصنوعی واقعی (کاملاً اختیاری)
اگه روزی به یه API دسترسی داشتی، فقط `AI_API_KEY` (و برای سرویس‌های سازگار با OpenAI: `AI_PROVIDER=openai`، `AI_BASE_URL`، `AI_MODEL`) رو تو Variables بذار. اون‌وقت گفتگو، راهنما و اخبار شهر اول از API جواب می‌گیرن و اگه API خطا داد یا سقفش پر شد **خودکار به مغز قانون‌محور برمی‌گردن**. ناظر گروه همیشه محلی می‌مونه.
متغیرهای سقف مصرف: `AI_USER_COOLDOWN_SECONDS` (۶)، `AI_USER_DAILY_LIMIT` (۲۵)، `AI_CHAT_DAILY_BUDGET` (۳۰۰۰)، `AI_NEWS_DAILY_BUDGET` (۳۰۰).

## ابزار ادمین (پیوی ربات، فقط ADMIN_IDS)
- `وضعیت هوش مصنوعی` → وضعیت مغز قانون‌محور و API
- `تست هوش مصنوعی` → یه تست سریع از گفتگو، راهنما و ناظر (یا تست اتصال API اگه وصل باشه)
