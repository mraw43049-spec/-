# 🤖 هوش مصنوعی روباهیو — راه‌اندازی

## چه قابلیت‌هایی اضافه شد
| قابلیت | چطور کار می‌کنه |
|---|---|
| 🦊 **چت با روباه** | تو پیوی ربات هر چی بنویسی روباه جواب می‌ده. تو گروه: `روباهیو سلام…` / `روباه جون …` / منشن کردن ربات / ریپلای روی جواب‌های روباه |
| 📖 **راهنمای هوشمند** | `راهنما <سوالت>` (مثلاً «راهنما کارت به کارت چطوره؟») — فقط از دانشنامه‌ی خود ربات جواب می‌ده، عدد و قانون از خودش نمی‌سازه |
| 🛡 **مدیریت هوشمند گروه** | ادمین گروه می‌نویسه `مدیریت هوشمند روشن` (پیش‌فرض خاموشه). توهین/نفرت‌پراکنی/تبلیغات **واضح** حذف می‌شه، سومین تخلف تو ۲۴ ساعت = ۱۰ دقیقه سکوت. ادمین‌ها بررسی نمی‌شن |
| 📰 **اخبار شهر** | تو گروه `اخبار شهر` — خبر بامزه از وضعیت شهر (از روی عددهای واقعی). با ارتقای شهر هم خودکار یه خبر شاد می‌آد |

## راه‌اندازی (۳ دقیقه)
1. یه کلید API بگیر (پیشنهاد: Anthropic — console.anthropic.com).
2. تو Railway → پروژه → **Variables** این‌ها رو اضافه کن:
   - `AI_API_KEY` = کلید API
3. دیپلوی که تموم شد، تو **پیوی ربات** (با اکانت ادمین) بنویس: `تست هوش مصنوعی`
   - اگه ✅ دیدی، همه‌چیز آماده‌ست.
   - اگه ❌ دیدی، متن خطا همون‌جا نوشته می‌شه (کلید اشتباه، اعتبار تموم، …).
   - `وضعیت هوش مصنوعی` مصرف امروز و تنظیمات رو نشون می‌ده.

> بدون `AI_API_KEY` همه‌ی این قابلیت‌ها بی‌صدا غیرفعالن و بقیه‌ی ربات مثل قبل کار می‌کنه.

## متغیرهای اختیاری
| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `AI_PROVIDER` | `anthropic` | `anthropic` یا `openai` (هر سرویس سازگار با OpenAI، مثل OpenRouter) |
| `AI_MODEL` | `claude-haiku-4-5-20251001` | برای `openai` حتماً تنظیمش کن |
| `AI_BASE_URL` | خودکار | مثلاً `https://openrouter.ai/api/v1` |
| `AI_ENABLED` | `1` | `0` = خاموش کردن کامل |
| `AI_USER_COOLDOWN_SECONDS` | `6` | فاصله‌ی بین پیام‌های هوشمند هر کاربر |
| `AI_USER_DAILY_LIMIT` | `25` | تعداد پیام هوشمند هر کاربر در روز |
| `AI_CHAT_DAILY_BUDGET` | `3000` | سقف کل پیام‌های چت/راهنما در روز (کل ربات) |
| `AI_MOD_DAILY_BUDGET` | `4000` | سقف کل بررسی‌های ناظر گروه در روز |
| `AI_MOD_CHAT_DAILY_LIMIT` | `800` | سقف بررسی هر گروه در روز |
| `AI_NEWS_DAILY_BUDGET` | `300` | سقف کل خبرهای شهر در روز |

## کنترل هزینه
- هر پیام هوشمند یه تماس API می‌شه؛ با مدل سبکِ پیش‌فرض هزینه‌ی هر پیام خیلی کمه، ولی برای اینکه هیچ‌وقت غافلگیر نشی سقف‌های بالا هست و **وقتی سقف پر بشه** ربات مؤدبانه می‌گه «فردا دوباره بیا» (بقیه‌ی ربات کار می‌کنه).
- شمارنده‌ها تو حافظه‌ان و با هر دیپلوی/ری‌استارت صفر می‌شن.
- اگه می‌خوای هزینه‌ی گفتگو رو به کاربر بدی (مثلاً روب‌پوینت کم بشه) بگو تا اضافه کنم.

## ایمنی و حریم خصوصی
- هوش مصنوعی هیچ ابزاری نداره: فقط متن جواب می‌ده و نمی‌تونه روب‌پوینت بده/بگیره، سطح عوض کنه یا کسی رو بن کنه.
- خروجی پاک‌سازی می‌شه (لینک و @منشن حذف می‌شن تا کسی ازش برای تبلیغ/پینگ سوءاستفاده نکنه).
- ناظر گروه **opt-in** هست و موقع روشن شدن به گروه اعلام می‌کنه که پیام‌های متنی برای بررسی به سرویس هوش مصنوعی فرستاده می‌شن (ذخیره نمی‌شن).
- ناظر فقط تخلف «واضح» رو اجرا می‌کنه؛ اگه مطمئن نباشه یا API خطا بده، هیچ کاری نمی‌کنه.
- برای حذف پیام، ربات باید ادمین گروه با دسترسی «حذف پیام‌ها» باشه (برای سکوت هم «محدود کردن اعضا»).

## نکته
دسترسی به API و روش پرداخت به کشور صاحب حساب بستگی داره؛ اگه با یه سرویس مشکل داشتی، `AI_PROVIDER=openai` + `AI_BASE_URL` + `AI_MODEL` رو برای یه سرویس دیگه تنظیم کن.
