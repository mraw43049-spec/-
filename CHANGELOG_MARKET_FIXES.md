# CHANGELOG_MARKET_FIXES

## ۱) «تخم مرغ پیدا نشد» در یخچال
دکمه‌ی «🥚 تخم مرغ‌ها» آرگومان `0` می‌فرستاد و هندلر قبل از رسیدگی به `list` دنبال تخم مرغی با id=0 می‌گشت و پیام «این تخم‌مرغ پیدا نشد» می‌داد.
حالا `list` قبل از جست‌وجوی تخم مرغ خاص رسیدگی می‌شود (`ruby_egg_button`).

## ۲) روباه زخمی خریداری‌شده به مجموع نجات‌یافته‌ها اضافه نمی‌شد
خرید فقط `injured_fox_stock` را زیاد می‌کرد. حالا `fox_rescued_count` (پروفایل + لیدربرد «روباه‌های زخمی») هم به اندازه‌ی تعداد خرید زیاد می‌شود.
(جمعیت/آمار خود شهر `city_rescued_total` تغییر نمی‌کند.)

## ۳) دارایی مورد نیاز خزانه برای ارتقای شهر
فرمول قبلی (۱۰۰ × ۳^سطح) جدول ثابت `CITY_TREASURY_REQ` شد و خیلی بیشتر است:

| ارتقا | خزانه لازم |
|---|---|
| ۱ → ۲ | ۱۰ هزار |
| ۲ → ۳ | ۵۰ هزار |
| ۳ → ۴ | ۲۰۰ هزار |
| ۴ → ۵ | ۷۵۰ هزار |
| ۵ → ۶ | ۲ میلیون |
| ۶ → ۷ | ۵ میلیون |
| ۷ → ۸ | ۱۲ میلیون |
| ۸ → ۹ | ۳۰ میلیون |
| ۹ → ۱۰ | ۷۵ میلیون |

برای تنظیم، فقط اعداد همین جدول را در `bot.py` عوض کن.
`fa_compact_number` هم حالا از یک میلیون به بالا «میلیون» و از یک میلیارد «میلیارد» می‌نویسد (مثلاً 5 میلیون، 1.25 میلیون).

## ۴) مالیات شهرداری از خریدار کسر نمی‌شد
قبلاً خریدار فقط قیمت محصول را می‌داد و مالیات از داخل همان قیمت جدا می‌شد.
حالا: **پرداخت خریدار = قیمت + ۵٪ مالیات**. کل قیمت محصول به خزانه شهر می‌رود و ۵٪ مالیات به موجودی شهردار.
(اگر شهردار نداشته باشیم، مالیات به خزانه می‌رود تا از بین نرود.)
مبلغ کل، مالیات و پرداخت نهایی در پنل سبد خرید و پیام موفقیت نشان داده می‌شود.

## ۵) پنل خرید مارکت مخصوص هر کاربر است
- آیدی صاحب پنل داخل `callback_data` تمام دکمه‌ها قرار گرفت (`rmarket:<action>:<chat>:<owner>[:e|f[:qty]]`).
- اگر کاربر دیگری دکمه را بزند: «⛔ این پنل مارکت برای کاربر دیگری است».
- دکمه‌ی «🛍 مارکت روبی» داخل پنل مشترک شهر/شهردار دیگر پیام مشترک را ویرایش نمی‌کند؛ یک پیام جدید مخصوص همان کاربر می‌سازد.
- پنل تنظیمات شهردار هم به شهردار متصل است.
- دکمه‌های پیام‌های قدیمی (فرمت قبلی) پیام «این پنل قدیمی است» می‌دهند.
