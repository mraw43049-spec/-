# نمایش آمار رفرال به خود معرف + آمار داخل پیام ادمین

## مشکل
درخواست تایید زیرمجموعه به‌درستی برای پشتیبانی می‌رفت، ولی خودِ کاربری که لینکش رو
فرستاده بود (معرف) هیچ خبری نمی‌گرفت و هیچ آماری نمی‌دید — مجبور بود خودش دستی دستور
«رفرال» رو بزنه تا ببینه کسی اومده یا نه. یعنی تنها اتفاقی که می‌افتاد، رفتن پیام تاییدیه
به پشتیبانی بود؛ هیچ بازخوردی به کاربر نشون داده نمی‌شد.

## راه‌حل
1. همین که یک نفر با لینک اختصاصی کاربری وارد بات می‌شه و رکورد رفرال (در انتظار تایید)
   ساخته می‌شه، یک پیام هم مستقیم برای خودِ معرف فرستاده می‌شه:
   - اسم/یوزرنیم فردی که با لینکش وارد شده
   - وضعیت «در انتظار تایید پشتیبانی»
   - آمار به‌روزشده‌ی خودش: کل زیرمجموعه / تاییدشده / در انتظار تایید
2. توی پیام تاییدیه‌ای که برای ادمین‌ها می‌ره هم یک خط آمار اضافه شد
   («📊 آمار این معرف: X کل | Y تاییدشده | Z در انتظار») تا ادمین موقع تصمیم‌گیری
   (تایید/رد) بدونه این معرف قبلاً چند نفر آورده.
3. توی «📊 آمار کلی» پنل مدیریت هم یک خط آمار کلی زیرمجموعه‌گیری (کل/تاییدشده/در انتظار)
   اضافه شد.

نتیجه: الان هم ادمین موقع تایید/رد، هم خودِ معرف بلافاصله بعد از دعوت، آمار رفرال رو
می‌بینن — نه فقط پیام تاییدیه‌ی خام بدون هیچ آماری.
