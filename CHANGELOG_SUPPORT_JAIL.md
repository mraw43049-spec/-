# CHANGELOG_SUPPORT_JAIL

## پنل پشتیبانی (/admin)
- دکمه‌ی «🚫 محرومیت کاربر» با «⛓️ زندان روبی» جایگزین شد.
  - «⛓️ انداختن کاربر در زندان»: در یک پیام بفرست `آیدی مدت دلیل`، مثلاً
    `123456789 2 ساعت اسپم در گپ` — مدت: دقیقه / ساعت / روز (یا `30m` `2h` `3d`)، حداقل ۱ دقیقه و حداکثر ۳۶۵ روز.
    اعداد فارسی هم قبول است و اگر فرمت غلط باشد همان‌جا دوباره می‌توانی بفرستی.
  - به کاربر در پیوی اطلاع داده می‌شود (دلیل، مدت، پایان حبس).
  - حبس پشتیبانی جریمه‌ی نقدی ندارد: در «زندان روبی» کاربر به‌جای جریمه پیام «حبس توسط پشتیبانی» می‌بیند، دکمه‌ی پرداخت جریمه نیست و با پول باز نمی‌شود. زندان قاچاق/اسپم مثل قبل جریمه دارد.
  - «✅ آزاد کردن کاربر»: زندان را برمی‌دارد و محرومیت‌های قدیمی (بن) را هم پاک می‌کند تا کسی گیر نکند.
  - زندان روی ادمین‌ها اعمال نمی‌شود (مثل قبل).
- دکمه‌ی «🦊 تنظیم روب‌پوینت» شد «🐾 تنظیم روب روب»: `آیدی + تعداد` → `fox_claim_count` تنظیم می‌شود.
  سطح کاربر (که همیشه از تعداد روب روب حساب می‌شود) هم همان لحظه تنظیم می‌شود؛ اگر سطح بالا برود جایزه‌ی سطح‌ها هم داده می‌شود و کاربر در پیوی خبردار می‌شود.
- «افزودن/کسر روب‌پوینت» و بقیه‌ی دکمه‌ها دست‌نخورده‌اند.
